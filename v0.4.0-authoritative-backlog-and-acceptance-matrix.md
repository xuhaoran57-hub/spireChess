# v0.4.0 权威待办与验收矩阵

- 状态快照：2026-08-05
- 当前代码候选：`0eaa2e371694598c3ab20686aea497731d98ab89`
- 冻结行为合同：[game-design-freeze-v0.4.0.md](game-design-freeze-v0.4.0.md)
- 当前工作入口：[TODO.md](TODO.md)

## 1. 文档地位与使用规则

本文是 **v0.4.0 唯一的当前执行待办和验收矩阵**。它将“实现存在”与“已完成
正式验收”分开记录，避免把历史候选、旧版本门禁和当前交付混在一起。

- 行为边界以 `game-design-freeze-v0.4.0.md` 为准；任何冲突都不得通过代码或本表
  静默决定。
- `TODO.md` 是本文的入口和历史索引；其中 2026-08-03 之前的阶段条目是实施记录，
  不再直接表示当前优先级。
- `development-plan.md` 保留为 v0.1 历史路线图，不用于判断 v0.4.0 完成度。
- 每项标为完成前，必须同时写明候选提交、运行环境、证据路径和结果。只存在代码、
  截图或旧候选结果时，不得写为“正式关闭”。

### 状态定义

| 标记 | 含义 |
| --- | --- |
| `已关闭` | 行为、自动化、所需人工/构建证据均已满足，且不会被本版本后续改动推翻。 |
| `实现待复验` | 当前代码或配置已有实现，但尚无 v0.4.0 当前候选的完整证据。 |
| `进行中` | 已有明确拆分和输入，允许继续实施。 |
| `待启动` | 尚未开始；开始前必须遵守本表的范围与验收条件。 |
| `暂停` | 仍在版本范围内，但根据项目负责人决定等待前置工作关闭。 |
| `外部依赖` | 需要项目负责人、第二台机器或未参与实现者参与；不得由工程实现单方面关闭。 |
| `暂缓/非范围` | 不属于 v0.4.0；不得作为当前版本的隐性工作。 |

## 2. 已知基线与关闭事实

| 项目 | 当前结论 | 证据 / 说明 |
| --- | --- | --- |
| v0.3.3 卡牌 Runtime 晋级 | 已关闭 | 51 项量产美术已晋级；签字与复验见 `phase-9c-v0.3.3-runtime-promotion-signoff.md`。 |
| v0.3.4 旧卡与 Token 刷新 | 已关闭 | 17 项已原位晋级，83/83 ArtId 覆盖，EditMode 385/385、PlayMode 30/30；见 `ui-concepts/phase-9c/light-storybook-production-v0.1/legacy-refresh-v0.3.4/README.md`。 |
| 三职业、局外档案、章节配置 | 实现待复验 | 由 `74efd66` 及后续 v0.4.0 提交落地；需要当前候选重新跑完整 Unity 与 Player 门禁。 |
| v0.4.0 遭遇曲线 | 已关闭 | 干净候选 `0eaa2e3` 已以 strict 门禁完成 19,800 场章节进度 S0：异常、效果上限、回合上限、P0、P1 均为 0，仅保留有意的 F1 教学首战 P2 饱和；提交、配置、fixture 与输出哈希见 `balance-results/v0.4.0/release-candidate-0eaa2e3/`。 |
| 日记式玩家流程与资产 | Clean Player 已关闭，外部资产签字待完成 | 候选 `d12e2ef` 已完成全量 Unity、Clean Windows x64、核心链与 Journal UI 双分辨率验收，见 `ui-concepts/unity-validation/v0.4.0-journal-ui/d12e2ef/`；十张 Bright Wandering Storybook 正式候选 Sprite（含锁定角色状态）的来源、哈希与离线核验见 `ui-concepts/phase-9c/light-storybook-production-v0.1/journal-ui-v0.4.0/`，来源/许可与外部视觉签字仍保持开放。 |
| MVP 战斗表现 | FX-A 实现待复验 | FX-A 已加入固定 32 节点图形池、攻击前摇/拖尾、命中爆点、受击闪白、轻重 hit-stop、棋盘震动、无矩形底伤害浮字及普通/Token 死亡差异；Prefab Builder 与运行时自愈绑定均已接入。当前本机 Unity 无有效许可，尚缺 Prefab 重建、全量测试、Player 对比视频和人工观感复核；FX-B/FX-C 未启动。三职业真实单局与平衡调整继续暂停。 |

## 3. 当前执行待办

执行顺序不可倒置：先完成玩家可见流程和 MVP 战斗表现，再在当前候选上重新建立
测试、Player 与平衡证据；否则后续截图、性能和试玩记录会因表现改动失效。

### P0-A：日记式主流程纵向切片

#### 2026-08-03 实现与 Clean Player 关闭快照

以下 UI 纵向切片已由干净候选 `d12e2ef` 完成 Unity、Clean Player 与双分辨率
工程验收；证据归档在 `ui-concepts/unity-validation/v0.4.0-journal-ui/d12e2ef/`。
外部资产来源/许可、第二机性能与外部试玩仍不能由本次工程验收替代。

| 范围 | 当前实现 | 仍缺少的关闭条件 |
| --- | --- | --- |
| 页面状态 | `JournalPageFlow` 约束 `Cover → Contents → HeroSelection → Map`，翻页期间锁输入，且有专用“跳过翻页”通道；`Map` 是交给既有路由前的终态转场页，同一进程回到目录不重播封面。 | 真机/Player 复验、可访问性复核。 |
| 建档与继续 | `MainMenuController` 仍在确认职业时调用既有 `GameApp.StartNewRun(heroId)`，继续仍走 `ContinueRun()`；翻页不承担领域提交。 | 干净候选下的保存/恢复与重复点击 XML/Player 证据。 |
| 章节与结局 | `RunTestController` 仅补充 `RunJournalPageState` 展示层：章节完成继续调用原有 `ContinueToNextFloor()`，结局返回目录调用既有 `SaveAndReturnToMainMenu()`；`tools/run_g4_acceptance.ps1 -JournalUi` 新增专用 Player 链，以确定性 `RunSession` 夹具准备章末状态、以真实 UI 完成选择/翻页/返回/继续。 | 三章真实 Player 截图和人工文案/可见性审核。 |
| 一次性解锁 | 读取已有 `UnreadUnlockNotifications` 后显示在章节页，并复用已有 `MarkUnlockNotificationRead`；无新局外档案格式。 | 幂等回归的全量执行结果与 Player 可见证据。 |
| v0.4.0 视觉资产 | 封面、目录、三名角色、三张章节图和结局图已作为 Sprite 接入既有 `Image` 槽位；资源、`.meta`、唯一 Style Tile、SHA-256 与离线校验见 `ui-concepts/phase-9c/light-storybook-production-v0.1/journal-ui-v0.4.0/`。缺图/未知 ID 仍回退中性占位；Prefab Builder 可输出 1920×1080、1920×1200 预览至 [v0.4.0-journal-ui](ui-concepts/unity-validation/v0.4.0-journal-ui/README.md)。 | Unity 导入、Player 双分辨率、来源/许可与人工视觉签字；候选不得在这些关闭前标记为 Runtime Ready。 |


| ID | 状态 | 工作项 | 完成条件 |
| --- | --- | --- | --- |
| V40-UI-01 | 实现待复验 | 建立 v0.4.0 日记资产包：封面、目录展开页、翻页正反面、纸张阴影、三名角色肖像、锁定状态、三张地图章节页和结局页。 | 十张需位图的正式候选已具备 `.meta`、唯一 Style Tile、哈希和版本清单；纸张、翻页和阴影继续用 UI 构件渲染。不改写 v0.3.3/v0.3.4 冻结资产；仍待来源/许可与视觉签字。 |
| V40-UI-02 | 实现待复验 | 将 MainMenu 改为显式页面状态机：首次进入 `Cover → Contents`，新游戏 `Contents → HeroSelect → Map`，返回目录不重播封面。 | 封面可跳过；翻页期间锁定相关输入；继续游戏直接恢复存档对应阶段；无确认角色时绝不创建单局。 |
| V40-UI-03 | 实现待复验 | 完成角色选择正式视图。 | 固定展示战士、法师、盗贼；已解锁角色显示正式肖像、被动和规则；未解锁角色显示锁定状态和条件且不可操作；当前单局不能换角。 |
| V40-UI-04 | 实现待复验 | 完成章节完成页和旅程结局页，并接入一次性解锁提示。 | 击败荒野/星轨高原 Boss 后先显示章节完成页和对应新角色提示，再进入下一图；击败铸魂熔城 Boss 后显示结局页；重复恢复、重试或重复点击不重复解锁或发被动。 |
| V40-UI-05 | 进行中 | 为新页面补齐 Prefab/序列化、输入、双分辨率和可访问性验证。 | 1920×1080、1920×1200 下无裁切、遮挡、失焦或不可点击控件；文本由 Unity UI 渲染；所有关键按钮有可测稳定名称。 |

### P0-B：当前候选的工程与验收基线

| ID | 状态 | 工作项 | 完成条件 |
| --- | --- | --- | --- |
| V40-QA-01 | 已关闭 | 在完成 P0-A 后，从干净 `HEAD` 运行全量 EditMode/PlayMode。 | 当前候选 `0eaa2e3`、Unity `2022.3.62f3c1`：EditMode 459/459、PlayMode 30/30，0 failed/skipped/inconclusive、无强制终止；XML、日志、哈希与时长见 `balance-results/v0.4.0/release-candidate-0eaa2e3/tests/`。 |
| V40-QA-02 | 已关闭 | 更新正式 Player 验收链，使其覆盖日记状态机而非直接点击旧角色页。 | `d12e2ef-clean-player` 为 `sourceTreeDirty=false` 的 Clean Windows x64 构建；核心链与 Journal UI 均在 1920×1080、1920×1200 通过，覆盖封面跳过、目录、新游戏、角色确认、章节完成、结局、保存返回与继续恢复。 |
| V40-QA-03 | 已关闭 | 新增重复输入与 exactly-once 回归。 | 当前候选全量测试覆盖翻页锁、重复确认、Boss/解锁通知幂等和三角色被动 exactly-once；Clean Player 再验证保存、返回、继续与路由只有一次。证据与测试名见 `ui-concepts/unity-validation/v0.4.0-journal-ui/d12e2ef/acceptance-summary.md`。 |
| V40-QA-04 | 已关闭 | 重新建立可追溯平衡证据。 | 候选 `0eaa2e3` 的 `metadata.json` 记录完整提交、`sourceTreeDirty=false`、配置哈希、fixture/来源 fixture SHA-256、固定种子、逐文件哈希与输出集合哈希；strict S0 198 场景/19,800 战斗通过，P0/P1 为 0，见 `balance-results/v0.4.0/release-candidate-0eaa2e3/`。 |

#### Clean Player 关闭记录：`d12e2ef`（2026-08-03）

- 候选：`d12e2ef0c528a697a4b57fa73f101dfe3059c6c5`，
  `sourceTreeDirty=false`，Unity `2022.3.62f3c1`，Windows x64。
- 配置哈希：`9ed1dbb542cffb31dab62aa339767bc4497ec7c858028cf36e3802c283646c7f`。
- 结果：EditMode 458/458、PlayMode 30/30、核心 Player 2/2、Journal Player
  2/2；两条 Player 链运行时错误均为 0。
- 证据：`ui-concepts/unity-validation/v0.4.0-journal-ui/d12e2ef/`，总清单、
  命令、哈希与复核边界见其中 `manifest.json` 和 `acceptance-summary.md`。
- 边界：该 `d12e2ef` 记录本身不包含章节进度 S0；`V40-QA-04` 已由下方
  `0eaa2e3` 独立证据包补齐。外部资产/性能/试玩签字仍按各自工作项保持开放。

#### 当前候选与章节 S0 关闭记录：`0eaa2e3`（2026-08-04）

- 候选：`0eaa2e371694598c3ab20686aea497731d98ab89`；Unity
  `2022.3.62f3c1`；配置哈希
  `9ed1dbb542cffb31dab62aa339767bc4497ec7c858028cf36e3802c283646c7f`。
- 全量测试：EditMode 459/459、PlayMode 30/30，0
  failed/skipped/inconclusive。
- S0 身份：`sourceTreeDirty=false`、`requireCleanSource=true`、
  `strictAcceptance=true`；fixture `0.4.0` SHA-256
  `471bf06e2d98176911001cafc39c745acb2ce04687a5b6105074e7120338ac36`；
  固定种子 `1000–1099`。
- S0 结果：198 场景/19,800 战斗；异常、效果上限、回合上限、P0、P1 均为
  0；仅保留 F1 教学首战 P2；`acceptancePassed=true`。
- 输出集合 SHA-256：
  `a854578eeaca51acd9613dac2f4a1d390e6f89e4e2848ef854698eb48a50c795`。
- 证据：`balance-results/v0.4.0/release-candidate-0eaa2e3/`；命令、边界、
  测试哈希与 P2 接受理由见其中 `acceptance-summary.md` 和
  `evidence-manifest.json`。
- 适用边界：本提交相对 `d12e2ef` 的非文档改动仅位于
  `Assets/Editor` 的 S0 取证命令与 `Assets/Tests` 的 fixture/回归断言，不改变
  Player 构建输入；因此已冻结的 Clean Player 与双分辨率证据继续适用。三角色真实
  单局和外部门禁仍独立开放。

### P0-C：MVP 战斗动画与特效美术

实施合同见
[`v0.4.0-mvp-battle-presentation-technical-design-v0.1.md`](v0.4.0-mvp-battle-presentation-technical-design-v0.1.md)。
该范围只扩展表现事件、动画、VFX 和取证，不修改规则、数值、牌池、遭遇、随机流或
存档格式。

| ID | 状态 | 工作项 | 完成条件 |
| --- | --- | --- | --- |
| V40-FX-01 | 已关闭 | 审计现有战斗播放并冻结 MVP 动画/特效美术方案。 | 方案明确现状、视觉语言、事件映射、实施批次、性能预算和人工验收门；项目负责人已确认表现优先于平衡。 |
| V40-FX-02 | 实现待复验 | 完成 FX-A 打击感基础。 | 工程实现已覆盖前摇、轨迹、命中爆点、闪白、轻重分级、短暂停顿、棋盘震动、无矩形底伤害浮字、普通/Token 死亡差异和 `SnapAndClear()` 强制归位；固定池上限 32，事件播放期不扩容。因本机 Unity 无有效许可，仍需在已激活 Unity 上重建 Prefab、执行全量 EditMode/PlayMode，并录制 1×/2×/跳过对比视频后关闭。 |
| V40-FX-03 | 待启动 | 完成 FX-B 法术与关键词语言。 | `DealDamage`、成长、护盾、召唤四类法术以及嘲讽、护盾、亡语、溅射、立即攻击有稳定、非文字主导的效果映射；三流派颜色/形状可区分。 |
| V40-FX-04 | 待启动 | 完成 FX-C 样板、自动化与双分辨率 Player 取证。 | 固定 Showcase 覆盖十类事件和四个关键词；全量测试、五轮嵌套亡语/连续召唤、1920×1080、1920×1200、1×/2×/跳过均通过且无残留。 |
| V40-FX-05 | 待启动 | 完成内部人工表现验收。 | 3 名观察者中至少 2 名无需日志即可辨认攻击、轻重、格挡、三类关键词和四类法术；无共同 P0/P1 可读性问题，项目负责人签字允许恢复真实单局。 |

### P1：v0.4.0 真实单局平衡候选

| ID | 状态 | 工作项 | 完成条件 |
| --- | --- | --- | --- |
| V40-BAL-01 | 暂停 | 建立 v0.4.0 平衡候选 Manifest。 | V40-FX-05 关闭后再启动；冻结提交、内容版本、规则版本、配置哈希、三角色、种子集、自动操作策略、排除规则和输出目录；不得复用内容 5.3.1 的 S1/S2 身份。 |
| V40-BAL-02 | 暂停 | 先跑三角色端到端小样本，验证真实商店经济、被动、路线和存档恢复。 | V40-FX-05 关闭后再启动；每个角色均覆盖三章、Boss 解锁、临时法术/偷取、护甲、正常继续和存档恢复；无确定性失败、效果上限或存档语义异常。 |
| V40-BAL-03 | 暂停 | 决定并执行正式 S1/S2 规模与人工体验批次。 | V40-FX-05 关闭后再启动；先根据 V40-BAL-02 的结论冻结样本量；人工记录至少覆盖构筑形成、三连、路线、时长、角色被动感知和难度突变；异常有明确接受或调数结论。 |

### P2：发布级外部门禁（不阻塞 P0-A/P0-B）

| ID | 状态 | 工作项 | 完成条件 |
| --- | --- | --- | --- |
| REL-AUD-01 | 外部依赖 | 正式 AI 音频生产与逐 Cue 听审。 | 3 首 BGM、25 个 P0 Cue/64 个变体具备来源、条款、Prompt、原始输出、母带、Runtime、循环样本和 SHA-256；通过 `ProductionStrict` 后才可标记 `ProductionApproved`。 |
| REL-VIS-01 | 外部依赖 | 五张已更新背景的生产许可与视觉签字。 | 签字前保持工程样板，签字后才可将对应资源标为 `Runtime Ready`；不追溯改变已关闭的 v0.3.3/v0.3.4 卡牌结论。 |
| REL-QA-01 | 外部依赖 | 第二台不同配置 Windows x64 的性能复验。 | 按归档执行包运行同一冻结构建；项目负责人明确接受或要求优化十卡 Shop 首次初始化尖峰，并签字冻结两机门槛。 |
| REL-QA-02 | 外部依赖 | 全量截图复核与外部试玩。 | 项目负责人完成当前候选双分辨率截图签字；至少 5 名未参与实现者提交非音频试玩记录。任何 P0/P1 必须清零；P2 必须记录接受理由或修复计划。 |

### 暂缓/非范围

- 通用数据驱动技能 DSL、第四角色、主动技能、天赋树和角色升级。
- 隐藏第四张地图的正式内容。
- 多存档槽、云存档与跨设备同步。
- 微信小游戏打包。
- Phase 9C 静态卡牌与背景之外的全量正式美术与动画。

## 4. 冻结验收矩阵

状态以“当前候选是否已经具备可交付证据”为准，而不是以历史版本是否曾通过为准。

| ID | 冻结验收条件 | 当前实现 / 证据 | 当前状态 | 关闭所需证据 |
| --- | --- | --- | --- | --- |
| AC-01 | 新安装仅开放战士；法师、盗贼可见但不可选。 | `ProfileProgressV1`、`HeroCatalog`、`ProfileProgressTests` 与角色卡状态已实现。 | 实现待复验 | 新安装 EditMode/PlayMode、正式角色页截图和点击禁用断言。 |
| AC-02 | 确认角色前不创建单局；确认后 Hero ID 可保存、恢复。 | `MainMenuController`、`GameApp.StartNewRun(heroId)`、快照映射和 `HeroJourneyAcceptanceTests` 已覆盖核心语义。 | 实现待复验 | 日记翻页路径的真实 UI 回归、保存/恢复和重复点击断言。 |
| AC-03 | 战士仅一次获得 10 护甲；战斗伤害先扣护甲，生命代价绕过。 | `HeroPassiveRules`、`RunSession` 与 `HeroPassiveTests` 已实现。 | 实现待复验 | 当前候选全量测试和 Player 可见护甲流程。 |
| AC-04 | 法师每个新商店至多一张合格临时法术，满备战区不补发。 | 职业独立随机流及 `HeroPassiveTests` 已实现。 | 实现待复验 | 当前候选全量测试；新开店、恢复、满备战区和 Boss 重试的 Player/日志证据。 |
| AC-05 | 盗贼仅偷可见剩余随从；三连、冻结和共享牌池一致。 | `ShopSession` 关闭前钩子和 `HeroPassiveTests` 已实现。 | 实现待复验 | 当前候选全量测试；冻结、满备战区、三连和恢复的端到端证据。 |
| AC-06 | 相同种子、角色、操作序列跨保存/恢复保持确定性。 | Hero 随机流进入快照，`HeroJourneyAcceptanceTests` 覆盖核心路径。 | 实现待复验 | 三角色的差分回归与 Player 恢复链结果。 |
| AC-07 | 两个 Boss 分别、幂等地解锁法师和盗贼。 | `ProfileProgressService`、`RunTestController` 与 `ProfileProgressTests` 已实现。 | 实现待复验 | 章节完成页一次性提示、重复 Boss/恢复不重复解锁的自动化与截图。 |
| AC-08 | 三张地图按“荒野→星轨高原→铸魂熔城”显示，遭遇符合主题。 | 配置、验证器和候选 `0eaa2e3` 的干净 strict S0 已建立；198 场景/19,800 战斗通过，P0/P1 为 0。`d12e2ef` 已归档第一章地图页双分辨率截图。 | 实现待复验 | S0 部分已满足；仍需第二、三章正式地图页双分辨率截图与视觉复核。 |
| AC-09 | 最终章节由配置决定，不依赖硬编码三层。 | `RunMap.IsFinalChapter` 和流程测试已实现。 | 实现待复验 | 配置驱动结局的 EditMode/PlayMode 与 Player 流程。 |
| AC-10 | v0.3.3 旧单局原样归档、不可继续，且不会被新单局/删除覆盖。 | `LegacyRunArchiveService`、原子档案和 `ProfileProgressTests` 已实现。 | 实现待复验 | 真实旧档目录迁移演练、删除 v0.4.0 单局后的归档完整性、目录提示截图。 |
| AC-11 | 封面/翻页期间重复点击不会重复建局或触发被动。 | `d12e2ef` 的 V40-QA-03 与 Clean Player 已通过并归档；`JournalPageFlow`、主菜单输入锁和 G4 PlayMode 双确认断言均为通过状态。 | 实现待复验 | 仅待 V40-UI-02 随 P0-A 的正式 UI/外部签字范围关闭；不得用本次工程验收替代该范围。 |
| AC-12 | 新规则有 EditMode、PlayMode、Clean Player 和双分辨率证据。 | `d12e2ef` 已归档 Clean Player、核心与 Journal 双分辨率证据；当前候选 `0eaa2e3` 另以 459/459 EditMode、30/30 PlayMode 和干净 strict S0 补齐最终自动化/平衡复核。 | 已关闭 | 工程证据已满足；外部资产、性能、视觉和试玩签字按其独立范围保持开放。 |

## 5. 验收运行顺序与产物

每个 v0.4.0 候选采用以下顺序；任一步失败都应停止后续签字并回到对应工作项。

1. **静态入口**：工作树干净，配置身份、JSON、资源 GUID/引用与更新资产清单通过。
2. **自动化**：运行 `tools/run_unity_tests.ps1 -Platform All`；保存 XML、日志和摘要。
3. **Player**：在隔离存档根运行 Clean Windows x64 构建；由真实 UI 点击覆盖 P0-A 的
   日记链路、三职业和章节/结局状态。
4. **双分辨率视觉**：在 1920×1080、1920×1200 捕获封面、目录、角色页、三章页、
   战斗/商店关键页、继续和结局；逐图复核文本、裁切、按钮遮挡和状态一致性。
5. **平衡**：在同一提交、同一配置哈希和冻结 fixture 上生成 S0；端到端样本和 S1/S2
   另建候选，不能混用。
6. **签字**：工程通过、视觉/性能外部证据和试玩各自签字；没有签字的外部项保持开放，
   不影响已关闭的独立范围。

建议将每次验收产物放在版本化目录，并在摘要中至少记录：候选提交、工作树状态、
Unity 版本、内容/规则版本、配置哈希、命令行、结果哈希、失败项和复验日期。

## 6. 变更控制

以下情形必须先更新冻结文档并重新确认，再修改实现或矩阵状态：职业行为、职业解锁
Boss、地图顺序/主题、旧档可继续迁移、隐藏第四图进入标准通关，以及战士初始 10 护甲。
数值调整不改变玩家可观察语义时，可以新建平衡候选，但不得覆盖已有结果目录或把旧
候选结论复制到新配置。
