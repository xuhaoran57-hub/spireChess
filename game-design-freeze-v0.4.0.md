# 《旅团日记》v0.4.0 游戏设计冻结

- 文档版本：v0.1
- 目标版本：v0.4.0
- 状态：Design Frozen
- 冻结日期：2026-07-31
- 适用范围：游戏名称、开场与主菜单流程、职业被动、职业解锁、三张正式地图、
  v0.3.3 旧单局存档处理

## 1. 文档地位

本文档是 v0.4.0 上述范围的权威设计输入。`game-design.md`、阶段文档、现有代码或
界面文案与本文冲突时，v0.4.0 以本文为准；未在本文中变更的商店、战斗、三连、
法术、遗珍、事件、地图节点和奖励规则继续沿用现有设计。

本文冻结的是玩家可观察规则和必须保持的数据语义，不要求重命名现有
`SpireChess` 命名空间、Editor 菜单或历史资产目录。内部重命名不得成为
v0.4.0 的前置条件。

## 2. 产品与叙事定位

### 2.1 游戏名称

游戏对外名称正式改为：**旅团日记**。

v0.4.0 必须更新玩家可见标题、主菜单、窗口标题和构建产品名。现有内部代码命名、
历史存档格式标识和工具菜单可以暂时保留，除非它们会直接显示给玩家或阻碍新功能。

### 2.2 核心叙事载体

整局旅程被表现为一本正在书写的旅行日记：

1. 启动游戏时显示日记封面；
2. 日记打开到目录页，目录页承载主菜单；
3. 选择“开始游戏”后翻页到职业/角色选择页；
4. 确认角色后翻页到第一张地图；
5. 击败第一、二张地图 Boss 后，通过章节完成页进入下一张地图；
6. 击败第三张地图 Boss 后进入本次旅程的结局页。

“继续游戏”只负责恢复已有单局，不重新进入角色选择，也不重复发放开局被动。

## 3. 主菜单与角色选择流程

### 3.1 日记页面状态

v0.4.0 的主菜单至少包含以下页面状态：

```text
封面打开 → 目录主菜单 → 角色选择 → 地图
```

- 封面动画只在进入主菜单时播放；允许点击跳过。
- 翻页期间锁定相关按钮，防止重复创建单局或重复路由。
- 返回目录时不重新播放完整封面动画。
- “继续游戏”从目录直接恢复存档对应阶段。
- 新单局只有在玩家确认一个已解锁角色后才创建并写入存档。

### 3.2 角色选择展示

角色页固定展示战士、法师、盗贼三个位置：

- 已解锁角色显示名称、肖像、被动名称和完整规则；
- 未解锁角色仍然可见，但不可选择，并显示明确解锁条件；
- v0.4.0 首次建立局外档案时只解锁战士；
- 单局创建后角色不可更换。

## 4. 职业系统公共规则

### 4.1 稳定身份

三个角色使用稳定 ID：

| 角色 | Hero ID | 初始状态 |
| --- | --- | --- |
| 战士 | `warrior` | 默认解锁 |
| 法师 | `mage` | 锁定 |
| 盗贼 | `rogue` | 锁定 |

角色名称可以本地化，Hero ID 不得因文案调整而变化。

### 4.2 单局约束

- 每个单局必须且只能绑定一个 Hero ID。
- Hero ID、职业运行时状态和职业随机流都必须进入单局存档。
- 读取存档不得重复执行 `OnRunStarted` 或 `OnShopStarted`。
- 相同内容版本、相同种子、相同角色和相同操作序列必须得到相同结果。
- 职业随机不得复用商店商品、普通奖励、事件或遗珍随机流，避免新增职业逻辑改变
  原有随机序列。
- v0.4.0 只实现下述三个明确被动，不建立通用技能脚本语言、主动技能、角色等级或
  天赋树。

## 5. 战士冻结规则

### 5.1 被动

被动名称暂定：**坚甲启程**。

> 创建战士单局时获得 10 点旅团护甲。旅团护甲优先抵挡战斗结算伤害。

### 5.2 护甲语义

- 初始护甲固定为 10，只在新单局创建成功时发放一次。
- 护甲是单局资源，跨节点、跨地图保留，并进入存档。
- 战斗结算伤害按下式处理：

```text
护甲吸收 = min(当前护甲, 战斗结算伤害)
结算后护甲 = 当前护甲 - 护甲吸收
生命损失 = 战斗结算伤害 - 护甲吸收
```

- 护甲抵挡普通、精英、Boss 失败以及平局产生的战斗结算伤害。
- 护甲不抵挡事件选项、遗珍或其他规则中明确写作“支付生命”“失去生命”或
  “降低最大生命”的代价。
- 治疗和篝火不恢复护甲；增加最大生命不增加护甲。
- Boss 重试、继续游戏和存档恢复不得再次补充 10 点护甲。
- 旅团护甲与随从关键词“护盾”是两个系统，不触发随从的获得护盾、失去护盾或
  护盾联动效果。
- 地图、战斗结算和继续游戏摘要必须能显示当前护甲。

## 6. 法师冻结规则

### 6.1 被动

被动名称暂定：**旅途灵感**。

> 每个商店阶段开始时，随机获得一张不高于当前酒馆等级的临时法术。

### 6.2 触发与候选池

- 每个实际打开的新商店阶段最多触发一次。
- 继续读取一个已经打开的商店不得再次触发。
- Boss 立即重试不属于新商店阶段，不触发该被动。
- 候选必须同时满足：已启用、可游玩、`shopEligible: true`、等级不高于当前酒馆
  等级。
- 三连发现系统法术和其他 `shopEligible: false` 的系统法术不得进入候选池。
- 所有合格法术等概率随机，使用独立的职业随机流。

### 6.3 获得与失效

- 法术免费获得，不扣金币，不占用商店法术商品位，也不视为“购买法术”。
- 法术进入普通备战区，可以按正常法术规则使用并触发“使用法术”联动。
- 法术带有临时标记；若本商店阶段未使用，商店关闭时自动移除。
- 备战区没有空位时，本阶段不获得法术，商店仍可正常进行。
- 无空位时不消耗职业随机数；界面必须给出一次清晰提示。
- 没有合格候选时不触发，并记录诊断信息，不阻断商店。

### 6.4 固定结算顺序

商店开始顺序冻结为：

1. 建立本回合基础经济并生成商品；
2. 应用延迟资源和遗珍的商店开始效果；
3. 结算法师被动；
4. 结算随从、法术及其他现有 `OnShopPhaseStart` 效果；
5. 允许玩家操作。

## 7. 盗贼冻结规则

### 7.1 被动

被动名称暂定：**顺手牵羊**。

> 商店阶段关闭前，从仍在商店中的随从里随机偷取一张。

### 7.2 触发条件

- 每个成功关闭的商店阶段最多触发一次。
- 候选只包括关闭时仍然可见、尚未购买的随从商品。
- 商店法术、Token、发现候选、奖励候选和全局牌池中未展示的随从不属于候选。
- 备战区至少有一个空位且存在候选时才触发。
- 备战区已满或商店中没有随从候选时，本阶段不触发；不阻止玩家结束商店，也不
  消耗职业随机数。
- 所有合格的可见候选等概率随机，使用独立的职业随机流。

### 7.3 卡牌与牌池语义

- 被偷取随从免费进入备战区，不扣金币，不视为购买，不触发 `OnBuy`。
- 被偷取随从从对应商品位移除，不再随其余商品返回共享牌池；该牌池副本继续由
  玩家持有。
- 被偷取随从是普通非金色实例，保留其正式配置，并接受现有适用于新获得随从的
  合法永久/旅团增益。
- 获得后立即执行现有三连检查；三连材料与牌池处理继续遵循现有规则。
- 若商店已冻结，只移除被偷取的商品；其余冻结商品继续保留到下一个商店阶段。

### 7.4 固定结算顺序

商店关闭顺序冻结为：

1. 校验尚未解决的发现、选择和其他阻塞状态；
2. 结算盗贼被动；
3. 按冻结状态保留或归还其余商品；
4. 结算现有 `OnShopPhaseEnd` 效果；
5. 移除临时法术并关闭商店；
6. 写入单局存档并继续地图流程。

该顺序要求实现独立的“商店关闭前”钩子，不能在商品已经归还牌池后通过现有
`OnShopPhaseEnd` 事件补做偷取。

## 8. 职业解锁冻结规则

### 8.1 解锁条件

| 角色 | 解锁条件 | 生效时点 |
| --- | --- | --- |
| 战士 | 建立局外档案时默认解锁 | 立即 |
| 法师 | 首次击败“荒野”Boss | 结算成功后写入局外档案 |
| 盗贼 | 首次击败“星轨高原”Boss | 结算成功后写入局外档案 |

### 8.2 解锁行为

- 解锁是账号本地的局外进度，与当前单局分离。
- 解锁写入必须幂等；重复击败 Boss 不产生重复记录或奖励。
- 解锁后当前单局角色不改变，新角色从下一次新游戏起可选择。
- Boss 胜利已经形成可持久结算时才解锁；预览、测试战斗、失败或未提交结算均不
  解锁。
- 解锁成功后在章节完成页显示一次通知；通知是否已读属于局外档案。
- 删除当前单局不得删除角色解锁。

## 9. 地图与章节冻结规则

### 9.1 正式地图名称

| 顺序 | 稳定 Map ID | 正式名称 | 主要敌对阵营 | 章节定位 |
| --- | --- | --- | --- | --- |
| 1 | `map_wilderness` | 荒野 | 荒灵 `WildSpirit` | 建立阵容、学习召唤与亡语压力 |
| 2 | `map_startrail_highlands` | 星轨高原 | 星契 `Starbound` | 检验法术、刷新、发现与永久成长 |
| 3 | `map_soulforge_city` | 铸魂熔城 | 铸魂 `ForgeSoul` | 检验护盾、嘲讽、反击与完整构筑 |

地图名称必须进入地图配置和存档摘要展示，不再由“第 N 层”“三层远征”等硬编码
文案代替。数字层数仍可作为进度和兼容字段使用。

### 9.2 路线与遭遇边界

- v0.4.0 继续采用现有完整可见的固定路线地图、节点选择和最终 Boss 模式。
- 商店、普通战斗、精英、事件、强化、休整和 Boss 节点类型继续保留。
- 每个标准战斗遭遇中，主题阵营必须占敌方阵容的明确多数。
- 旅团 `Wayfarer` 可以作为跨地图雇佣兵、向导或支援单位出现。
- 非本地图的另外两个主阵营只允许在有明确叙事说明的混合遭遇中出现，且不能取代
  当前地图主题。
- 每张地图的标志性精英和 Boss 必须以主题阵营机制为核心，不能使用无主题的终局
  混编阵容代替。
- 现有随从配置可以复用，但三层遭遇阵容、数值加成、名称、主题、风险提示和 Boss
  奖励必须按新顺序重新校准。

### 9.3 地图推进

- 击败“荒野”Boss 后进入章节完成页，再进入“星轨高原”。
- 击败“星轨高原”Boss 后进入章节完成页，再进入“铸魂熔城”。
- 击败“铸魂熔城”Boss 后完成 v0.4.0 正式旅程并进入结局页。
- 地图最大层数必须由正式地图/旅程配置决定，禁止继续用数值 `3` 判断是否通关。

### 9.4 隐藏第四张地图

隐藏第四张地图不属于 v0.4.0 的交付范围。其名称、新种族、敌人、进入条件、是否
替代结局以及是否计入标准通关均未冻结；v0.4.0 只要求地图推进代码不再依赖固定
三层，以便后续扩展。

## 10. 单局与局外存档策略

### 10.1 v0.3.3 旧单局

v0.3.3 进行中的单局**不迁移为可继续的 v0.4.0 单局**，原因包括：

- 三张地图的身份和遭遇内容发生变化；
- v0.4.0 单局必须绑定角色并保存职业运行时状态；
- 新增护甲和独立职业随机流后，旧战斗结果无法可靠重放；
- 当前存档使用严格内容版本、规则版本和配置哈希校验。

v0.4.0 首次启动发现旧单局时必须：

1. 在不修改原文件内容的前提下复制主存档、备份及可识别的修复文件到只读语义的
   `Legacy/v0.3.3/<timestamp>/` 归档目录；
2. 不自动删除或覆盖旧存档；
3. 在目录页明确提示“旧旅程已安全归档，v0.4.0 需要开始新旅程”；
4. 不提供强行加载、忽略内容哈希或为旧单局补选角色的入口；
5. 删除 v0.4.0 当前单局时不得连带删除旧版本归档。

如果目标设备上从未存在 v0.3.3 单局，则直接创建新的 v0.4.0 局外档案。

### 10.2 v0.4.0 单局存档

v0.4.0 使用新的单局 Schema，至少新增：

- `heroId`；
- 当前旅团护甲；
- 职业随机流状态；
- 已处理的职业阶段触发标记；
- 语义化 Map ID。

内部存档格式 ID 可以为降低改动风险而继续沿用，但 Schema 必须能明确拒绝缺少上述
字段的旧单局。存档恢复必须保持职业被动 exactly-once。

### 10.3 局外档案

新增独立的 `ProfileProgressV1`，至少保存：

- 已解锁 Hero ID 集合；
- 已击败的章节 Boss；
- 尚未阅读的解锁通知；
- Profile Schema 与最后写入版本。

局外档案采用原子写入和备份，不得依赖当前单局文件，也不得只保存在
`PlayerPrefs`。v0.3.3 没有职业解锁数据，因此首次迁移结果固定为“仅战士解锁”。

### 10.4 存储路径与改名

当前运行时依赖 `Application.persistentDataPath`，而更改 Unity 产品名可能改变实际
目录。v0.4.0 必须引入稳定、与本地化展示名称分离的存储身份，并在切换到新路径前
执行一次旧路径探测与归档。

此后仅修改玩家可见标题不得再次改变存档根目录。旧路径探测失败不能阻止新游戏，
但必须记录诊断信息。

## 11. 美术与声音边界

- v0.4.0 继续使用 v0.3.3 的明亮旅行绘本作为唯一风格基线。
- 新日记封面、展开页、翻页正反面、纸张阴影、角色肖像、锁定状态和地图章节页必须
  建立 v0.4.0 资产版本，不静默改写 v0.3.3 冻结证据。
- 日记页保持暖象牙纸、开放日光、低细节 UI 留白和清楚的主体—背景分离。
- 不在生成背景中烘焙按钮、可读文字、数值或 Logo；正式文字由 Unity UI 渲染。
- 翻页动画必须有可跳过路径，且不承担任何关键状态提交。
- v0.4.0 不以正式配音、角色语音或新音乐为交付前置条件。

## 12. 验收标准

以下条件全部满足后，本文冻结范围才算实现完成：

1. 新安装只开放战士，法师和盗贼可见但不可选择；
2. 新单局在确认角色前不会创建，确认后 Hero ID 可保存并恢复；
3. 战士只获得一次 10 点护甲，战斗伤害先扣护甲，生命代价绕过护甲；
4. 法师每个新商店至多获得一张合格临时法术，满备战区不重复补发；
5. 盗贼只偷剩余可见随从，获得、三连、冻结和共享牌池数量一致；
6. 相同种子、角色和操作序列在保存/恢复前后保持确定性；
7. 击败荒野和星轨高原 Boss 后分别、幂等地解锁法师和盗贼；
8. 三张地图依次显示“荒野”“星轨高原”“铸魂熔城”，主题遭遇满足本文边界；
9. 第三张地图是否结束旅程由配置决定，不再依赖硬编码层数；
10. v0.3.3 旧单局被原样归档、不可继续且不会被新单局或删除操作覆盖；
11. 封面和翻页期间重复点击不会创建多个单局或重复触发职业被动；
12. 新增规则具备 EditMode、PlayMode、Clean Player 与双分辨率关键画面证据。

## 13. 明确不在 v0.4.0 范围内

- 隐藏第四张地图的正式内容；
- 第四个角色、主动技能、天赋树或角色升级；
- 通用数据驱动技能 DSL；
- 多存档槽、云存档或跨设备同步；
- 将全部 `SpireChess` 代码命名、Editor 菜单和历史文件批量改名；
- 为 v0.3.3 进行中单局提供可继续迁移；
- 全新种族、全新商店经济或战斗规则重做。

## 14. 变更控制

以下任一变化需要更新本文版本并重新确认，不能在实现中静默决定：

- 战士护甲抵挡生命代价或可被常规治疗恢复；
- 法师法术改为永久卡、改变候选等级或占用商店商品位；
- 盗贼改为从全局牌池偷取、允许满备战区暂存或触发 `OnBuy`；
- 调整法师、盗贼解锁 Boss；
- 修改三张正式地图名称或主题阵营顺序；
- 尝试把 v0.3.3 单局自动升级为可继续单局；
- 将隐藏第四张地图纳入 v0.4.0 标准通关。

数值平衡可以在不改变上述玩家可观察语义的前提下迭代，但战士初始 10 点护甲属于
本次冻结数值，修改时同样需要更新本文。
