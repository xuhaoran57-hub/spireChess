# v0.4.0 可审看战斗 Showcase 技术方案 v0.1

> 实施状态（2026-08-06）：Showcase 事件、候选 VFX 与预制体构建接入已完成；仍待有有效 Unity 许可证的 Editor/Player 验收。

- 日期：2026-08-06
- 状态：提案，待评审
- 上位方案：v0.4.0-mvp-battle-presentation-technical-design-v0.1.md
- 视觉基线：Bright Wandering Storybook / 暗色奇幻棋盘
- 目标场景：sc/Assets/Scenes/BattleTest.unity，含一段真实 Shop OnPlay 前置段

## 1. 决策

本方案将“可审看战斗 Showcase”定义为一个可重复运行、由真实领域事件驱动的 45–60
秒演示，而不是一段离线合成视频或一张静态概念图。

它的唯一目标是验证玩家能否不看战斗日志，仍然读懂攻击、护盾、战吼、亡语、召唤和
溅射的因果关系与轻重。它不用于调平衡，不修改卡牌规则、数值、随机流、存档或正式
内容池。

参考同类卡牌游戏时只借鉴“预示 → 动作 → 命中 → 结果 → 收束”的信息节奏；不得复制
炉石传说的角色、美术、图标、音效、文本或具体镜头资产。

### 1.1 完成定义

Showcase 达到以下条件才可供评审：

1. 在真实 Shop 和 Battle 领域路径上触发指定事件，不伪造最终状态；
2. 普通攻击、护盾格挡、战吼、亡语召唤、溅射各有可辨识的非文字主导反馈；
3. 1×、2×、跳过产生相同的领域结果，跳过后没有残留特效、位移、闪白或音效；
4. 1920×1080、1920×1200 下不遮挡攻防数字、名称和战斗日志安全区；
5. 固定池不扩容，且完整 Showcase 结束后所有瞬态数量归零；
6. 三名内部观察者中至少两名可在不看日志的情况下说出每个关键事件的来源、目标与
   结果。

### 1.2 非目标

- 不为每张随从或法术制作专属动画；
- 不引入 Timeline、骨骼动画、VFX Graph、3D 摄像机或全局后处理；
- 不把静态效果图烘焙进战斗截图；
- 不把候选音效标记为正式 ProductionApproved；
- 不因 Showcase 修改平衡、事件顺序或领域结算。

## 2. 演示结构与真实路径

Showcase 由四个短段落组成。每段使用固定种子、真实 Config、真实 ShopSession 或
BattleSimulator.SimulatePlayback() 路径。段落之间可以重置测试局面，但不得使用
“直接替换 UI 状态”的伪造方式。

| 段落 | 实际来源 | 必须可见的结果 | 目标时长（1×） |
| --- | --- | --- | --- |
| S0：战吼入场 | ShopTest：打出 traveling_physician，选择真实战斗区目标 | 单位落位、来源印章、到目标的联系、目标生命成长 | 4–6 秒 |
| S1：攻击、嘲讽与护盾 | BattleTest：mercenary_shieldbearer 与非旅团相邻，触发 OnBattleStart 护盾；敌方首先命中该嘲讽单位 | 嘲讽被迫选中、护盾生成、第一次攻击被格挡、破盾 | 12–16 秒 |
| S2：重击与溅射 | BattleTest：使用已验证会获得 Cleave 的 golden_mirrorsteel_duelist 或等价真实配置 | 主目标接触、两侧次目标延迟受击、伤害层级不同 | 10–14 秒 |
| S3：亡语链与 Token | BattleTest：fox_den_matriarch 死亡，依次生成 token_two_tailed_fox_shadow 和两个 token_young_spirit | 普通死亡、亡语印章、召唤、Token 死亡/消散、连续清理 | 18–24 秒 |

S0 是战斗 Showcase 的前置小节，而不是把战吼伪装成战斗事件。战吼发生在 Shop
阶段，应当由 ShopSession 的 OnPlay 结果驱动；S1–S3 由 BattleSimulator 的播放事件
驱动。这样评审的视觉结果与真实玩法一致。

### 2.1 固定演示顺序

1. ShopTest 打出战吼单位并选定目标，保留效果完成后的 700ms 阅读窗口；
2. 载入 BattleTest，显示战斗开始与嘲讽/护盾准备；
3. 播放一次被护盾格挡的攻击与一次护盾破裂后的有效伤害；
4. 播放一次重击或致命攻击，并紧接一次溅射的主、次目标结算；
5. 播放狐影亡语召唤链，最后保留 800ms 的棋盘稳定画面；
6. 结束后展示结果层，再返回干净的 BattleTest 初始状态。

每段须具备稳定的检查点名称，建议为：

    showcase-warcry
    showcase-shield-block
    showcase-shield-break
    showcase-cleave-primary
    showcase-cleave-secondary
    showcase-deathrattle-seal
    showcase-summon-chain
    showcase-cleanup

## 3. 表现语法和时序

所有时间均为 1× 基线。2× 按相同比例压缩；跳过不等待任何协程，而是调用既有
SnapAndClear() 路径使视图立即到达领域已结算状态。表现协程不得修改 Time.timeScale。

| 事件 | 预示 | 动作与接触 | 结果与收束 | 总阻塞时长 |
| --- | --- | --- | --- | --- |
| 普通攻击 | 攻击者缩至 0.96，目标轮廓脉冲 60ms | 90ms 突进和 3 条短光带；接触帧 35ms 出爆点/闪白 | 回弹 110ms；伤害浮字独立上飘 420ms | 295ms |
| 重击/斩杀 | 同普通攻击，追加更亮的攻击圆章 | 8–12 个冲击元素；45/60ms 表现停顿 | 目标较强抖动，死亡在伤害后才开始 | 315ms |
| 护盾获得 | 目标轮廓亮起 60ms | 天青环由内向外扩张 120ms | 常驻护盾层淡入，160ms 后回到静默 | 180ms |
| 护盾格挡/破裂 | 护盾层短亮 35ms | 格挡出现六边切角；破裂则先出现裂纹后飞散 | 格挡用蓝白小数值或图标；破裂后才允许红色伤害反馈 | 150/230ms |
| 战吼 | 单位落位后来源印章预热 80ms | 印章亮起，联系线在 120ms 到达目标 | 目标成长环和 +X/+Y，保留 180ms | 380ms |
| 亡语 | 单位压暗并缩至 0.92，80ms | 160ms 消散；亡语封印裂开 110ms | 结果事件结束后才播放召唤 | 270ms + 结果 |
| 召唤 | 空槽位亮环 60ms | 竖光和尘雾 120ms，单位 0.72→1.08 | 90ms 回弹到 1.0；连续召唤逐个串行 | 270ms/单位 |
| 溅射 | 主攻击用宽弧拖尾 | 主目标命中后 +40ms 拉出两条分支；次目标依次命中 | 次目标爆点缩小为主目标的 70%，各自独立浮字 | 260–340ms |

轻击、重击、致命/关键词链的表现停顿分别为 25ms、45ms、60ms。停顿只暂停当前
表现序列，规则已经完成结算且不会因此改变确定性。

### 3.1 空间层规则

| 层级 | 可放内容 | 禁止内容 |
| --- | --- | --- |
| 立牌表面 | 护盾染色、受击闪白、短时命中爆点 | 名称条、攻击/生命数字上连续遮挡超过 100ms |
| 立牌外框 | 嘲讽脉冲、战吼来源印章 | 长时间常驻的光带或大面积遮罩 |
| 关系层 | 攻击拖尾、战吼联系线、溅射分支 | 静默状态下的常驻大弧线 |
| 棋盘层 | 落位环、召唤门、低幅震动 | 全屏闪白、遮挡日志面板 |
| UI 浮层 | 伤害/+X/+Y 浮字、结果层 | 带矩形底的大型文本卡片 |

全屏闪光峰值 Alpha 不得超过 0.22。静默关键词中，激活者可保持 100% 亮度，其他
常驻关键词降至约 30%，但名称、攻击、生命始终为禁入区。

## 4. 素材策略

先用当前 FX-A 的 UGUI 图形池验证时序和事件映射，再把已确认的槽位替换为候选 Sprite。
候选 Sprite 用于“可审看”版本；它们不是 83 张卡的全量生产资产。

### 4.1 复用候选资产

下列资产已存在于 ui-concepts/phase-9b/battle-keyword-assets-v0.1/，应优先按当前
空间语言复用，而非重新生成重复稿。接入 Runtime 前仍须逐项建立来源、哈希、导入与
许可记录。

| 资产组 | 复用内容 | 运行时用途 |
| --- | --- | --- |
| 护盾图集 | shield 的六帧关键姿势 | 获得、格挡、裂纹、破碎；常驻层继续沿用现有护盾覆盖 |
| 亡语图集 | deathrattle 的六帧裂蜡封/触发环 | UnitDied 后、结果 Action 前的 110–180ms 封印触发 |
| 溅射图集 | splash 的展开、主命中、分支命中和涟漪参考帧 | 主命中点材质；两条分支由运行时按实际次目标拉伸 |
| 关键词静默层 | 现有嘲讽底座、亡语封印、溅射标记、护盾覆盖 | 静默识别；激活时只做短脉冲，不替换布局 |

### 4.2 需生成的最小候选素材包

需新生成 14 个透明底源 PNG；其中图集可切出多个 Sprite，但不得直接生成“完整卡面”
或带文字的效果截图。所有素材均为独立、可着色、可缩放的局部元素。

| ID | 类型与尺寸 | 数量 | 用途 | 视觉说明 |
| --- | --- | ---: | --- | --- |
| FX_ATK_TRAIL_LIGHT | 1024×256 PNG | 1 | 普通攻击轨迹 | 金黄斜切纸墨光带，头尖尾淡 |
| FX_ATK_TRAIL_HEAVY | 1024×256 PNG | 1 | 重击/致命攻击 | 更粗、更短、更集中的暖白金冲刺痕 |
| FX_CLEAVE_ARC | 1024×256 PNG | 1 | 溅射关系线 | 无文字的分叉弧线，运行时裁剪并连到次目标 |
| FX_HIT_LIGHT | 512×512 PNG | 1 | 轻击接触点 | 四至六条短放射线和小纸屑点 |
| FX_HIT_HEAVY | 512×512 PNG | 1 | 重击/斩杀接触点 | 菱形爆点、裂纹和更密的短线 |
| FX_WARCRY_SEAL | 512×512 PNG | 1 | 战吼来源 | 暖金/流派色的无字印章，供 OnPlay 和 EffectTriggered 使用 |
| FX_WARCRY_LINK | 1024×128 PNG | 1 | 战吼对目标关系线 | 细丝带/墨线，起点和终点可拉伸 |
| FX_STAT_GROWTH | 512×512 PNG | 1 | 成长与增益 | 上升刻度、叶片短线与金色粒点，支持流派 Tint |
| FX_DEATH_DISSOLVE | 512×512 PNG | 1 | 普通单位死亡 | 灰烬方片和向内收束边缘，不含角色图像 |
| FX_TOKEN_POOF | 512×512 PNG | 1 | Token 消散 | 轻量纸屑/烟尘，持续更短且元素更少 |
| FX_SUMMON_PORTAL | 512×512 PNG | 1 | 召唤入场 | 圆形纸墨传送门或封印环 |
| FX_SUMMON_BEAM | 256×1024 PNG | 1 | 召唤竖光 | 窄束、中心亮、两侧透明，供目标槽位拉伸 |
| FX_SUMMON_DUST | 512×512 PNG | 1 | 召唤落位尘雾 | 6–8 帧小图集，向上消散 |
| FX_EFFECT_BOLT | 1024×192 PNG | 1 | 伤害/通用效果传播 | 可 Tint 的短投射体，用于非普攻的 EffectTriggered |

三流派使用 Tint 和同一形状语言，不额外生成三套重复素材：

| 流派 | Tint | 形状偏好 |
| --- | --- | --- |
| 铸魂 | #E46A32 与暖金 | 厚环、火星、短直线 |
| 荒灵 | #69B86B 与 #D6D36A | 叶片、孢子、柔弧 |
| 星契 | #6EC5E9 与 #A986FF | 菱形、轨道、细刻度 |

### 4.3 生成约束与提示词骨架

所有图片生成均使用唯一的 Bright Wandering Storybook Style Tile 作为视觉参照，并采用
如下不可省略的约束：

    isolated 2D game VFX element, bright wandering storybook,
    hand-painted watercolor and ink, transparent background,
    no character, no card frame, no UI text, no letters, no logo,
    centered with safe transparent padding, crisp readable silhouette

每个资产再追加其表中的颜色、形状和用途。例如 FX_CLEAVE_ARC 必须补充“single
branching arc only, no targets, no damage numbers”，避免生成模型把单位、文字或完整
战斗画面烘焙进去。

生成合格条件：

- PNG 为 RGBA，背景 Alpha 为 0；不接受色键背景或未处理的黑底，护盾 Additive 旧资产
  除外；
- 外缘预留至少 8% 透明安全边，不能被裁切；
- 没有文字、数字、商标、卡框、角色面孔或与其他作品可识别的特征；
- 缩至 Compact 立牌宽度的 35% 后仍能读出轮廓；
- 产物随附原始输出、Prompt、模型/工具、生成日期、SHA-256 与人工筛选结论。

### 4.4 导入与运行时材料

候选原件的工作区放在：

    ui-concepts/phase-9c/battle-showcase-v0.1/

经筛选后的 Runtime 副本放在：

    sc/Assets/Art/Presentation/UI/Battle/Vfx/

导入设置统一为：

- Texture Type：Sprite (2D and UI)，Sprite Mode：Single 或预先声明的 Multiple；
- Mesh Type：Full Rect，Mip Maps：关闭，Alpha Is Transparency：开启；
- sRGB：开启；透明 PNG 不使用色键；UI 缩放以 100 PPU 为起点；
- 标准拖尾、封印、碎片使用 Alpha 混合；局部高光、爆点和传送门使用派生自既有护盾
  配置的 UI Additive 材质；
- 不在播放循环加载 Resources，不在播放期 Instantiate/Destroy。

不新建泛用资源 DSL。应在现有 PresentationSpriteCatalog 中增加一个 Battle Vfx 分组，
以显式字段引用这 14 个候选槽位；缺图时回退到当前 UGUI 形状，而不是静默丢失效果。

## 5. 音频方案

Showcase 音频为 Commissioning 候选，不替代正式音频生产门禁。使用已有
PresentationAudioCatalog 和 AudioPlaybackLimiter；先为每个需要的 Cue 提供一个原始
候选，再按现有 Catalog 的变体数要求扩充。

### 5.1 复用与新增 Cue

| Cue | 处理 | 候选时长 | 表现时机 |
| --- | --- | ---: | --- |
| battle_attack_light | 替换为 1 个轻挥击候选，后续补足现有 4 个变体 | 180–240ms | 攻击前摇至突进 |
| battle_hit | 替换为 1 个纸墨/木质命中候选，后续补足 4 个变体 | 90–150ms | 主目标接触帧 |
| battle_shield_gain | 复用 ID，补 1 个柔和环鸣候选 | 220–320ms | ShieldGained |
| battle_shield_break | 复用 ID，补 1 个玻璃/纸片破裂候选 | 250–420ms | 格挡破裂或 ShieldLost |
| battle_stat_up | 复用 ID，补 1 个轻上行铃音候选 | 220–360ms | 战吼/成长结果 |
| battle_death | 复用 ID，补 1 个低频纸灰散落候选 | 300–450ms | 普通 UnitDied |
| battle_token_death | 复用 ID，补 1 个更轻、更短的纸屑消散候选 | 120–220ms | Token UnitDied |
| battle_summon | 复用 ID，补 1 个吸入后展开的入场候选 | 350–520ms | UnitSummoned |
| battle_warcry | 新增 3 个变体 | 280–420ms | OnPlay 来源印章亮起 |
| battle_cleave | 新增 3 个变体 | 180–300ms | 主命中后分支扩散 |

普通与重击的差别由 battle_hit 的音量、低通/音高微调和画面分级共同承担；首版不新增
每张卡独有 Cue。若评审仍无法听出重击，可在 FX-B 追加 battle_attack_heavy，而不在
Showcase 阶段预先扩大 Cue 表。

### 5.2 音频技术约束

- 源文件：48kHz、24-bit WAV；Runtime 按现有项目导入策略压缩，保留无损源文件；
- 峰值不超过 -3 dBFS；Showcase 不用长混响遮盖事件先后；
- 同时最多 4 个非循环战斗 SFX；同类事件在 80ms 内只允许一个主 Cue；
- 护盾格挡不用 battle_hit 的红色受击音，破盾可叠加 battle_shield_break；
- 跳过立即停止所有非循环 Showcase SFX，不能等到自然尾音结束；
- 每个候选需要来源、Prompt/录制方式、母带、Runtime 文件和 SHA-256。

## 6. 运行时接入

### 6.1 领域与表现边界

BattleSimulator 继续决定所有规则和结算；表现层只能消费领域已产生的事件。不得因
播放、音效、帧率或跳过而重算伤害、目标、召唤位或胜负。

FX-B 前需扩展 BattlePlaybackEvent，使其携带只读表现元数据：

| 字段 | 用途 | 不得影响 |
| --- | --- | --- |
| EffectId | 对应实际 EffectConfig 的 ID | 随机流、存档、数值 |
| EffectTrigger | OnBattleStart、OnDeath、OnSummon 等来源 | 领域执行顺序 |
| EffectAction | AddShield、ModifyStats、SummonToken、ImmediateAttack 等 | 规则判定 |
| IsSplashDamage | 标明次目标伤害 | 伤害计算 |
| IsImmediateAttack | 标明召唤后立即攻击 | 攻击资格 |
| PresentationVariant | Normal、Strong、Critical 或流派 Tint 选择 | 结算与平衡哈希 |

需要新增 EffectTriggered 播放事件，位置必须在真实效果执行和对应结果事件之间：

    领域触发效果
    → EffectTriggered（来源印章/联系线）
    → DamageApplied / ShieldGained / StatsChanged / UnitSummoned

亡语必须保持：

    UnitDied
    → EffectTriggered（亡语封印）
    → UnitSummoned 或 StatsChanged

### 6.2 组件职责

| 组件 | 保持职责 | Showcase 所需小改动 |
| --- | --- | --- |
| BattleImpactFxLayer | 固定池管理图形条目 | 增加 Sprite/材质槽位和配方方法；容量仍为 32 |
| BattleScreenView | 串行播放 BattlePlaybackEvent | 按元数据选择配方、处理时序和棋盘震动 |
| BattleStandeeView | 立牌状态与关键词层 | 增加战吼/亡语/嘲讽脉冲，保证 Snap 恢复 |
| PresentationFxPool | 文本浮字 | 保留，只去除大矩形底并支持伤害/增益颜色 |
| PresentationSpriteCatalog | 现有卡牌/立牌资源 | 增加 Battle Vfx 显式槽位，不另建通用 Catalog |
| PresentationAudioCatalog | Cue 与变体校验 | 注册 battle_warcry、battle_cleave 及候选状态 |
| BattleUiPrefabBuilder | 生成并串接 Battle UI | 序列化 Vfx Catalog、Additive 材质与 Showcase 路径 |
| G4PlayerAcceptanceRunner | 正式 Player 取证 | 给既有入口增加 Showcase 模式，不另建平行验收程序 |

### 6.3 池预算

BattleImpactFxLayer 的最大可见条目继续固定为 32。单一事件的图形预算不得超过 12：

| 配方 | 最大图形条目 |
| --- | ---: |
| 普通攻击 | 3 拖尾 + 5 爆点/碎片 = 8 |
| 重击/致命 | 3 拖尾 + 9 爆点/碎片 = 12 |
| 护盾生成/破裂 | 1 环 + 6 切片 = 7 |
| 战吼/成长 | 1 印章 + 1 联系线 + 4 刻度 = 6 |
| 亡语 | 1 封印 + 4 灰烬 = 5 |
| 召唤 | 1 门 + 1 竖光 + 4 尘雾 = 6 |
| 溅射 | 1 主爆点 + 2 分支 + 2×3 次目标元素 = 9 |

当池满时，可丢弃最早的低优先级尘雾和粒点，但不可丢弃主目标命中、护盾破裂、死亡、
召唤、关键浮字或结果层。不得动态扩容。

## 7. 自动化、Player 取证与人工审看

### 7.1 自动化

新增或扩展下列断言：

1. Showcase 的每个段落能由真实 Shop/Battle Config 产生预期事件类型；
2. 每个 EventKind + EffectAction 映射到唯一、稳定的 VFX 配方与 Cue；
3. 格挡没有红色伤害浮字或 battle_hit 生命损失语义；
4. 亡语封印发生在 UnitDied 后、其结果事件前；
5. 溅射次目标相对主目标有至少 40ms 的可观测延迟；
6. 五轮嵌套亡语/连续召唤以及 1×、2×、跳过后，领域结果哈希一致；
7. SnapAndClear() 后所有池、CanvasGroup、位移、缩放、闪白、关键词脉冲和非循环音频
   均为零；
8. 1920×1080 与 1920×1200 捕获中没有遮挡关键数值、未导入资源或运行时实例化。

现有 BattleUiPrefabTests、BattleUiPlayModeTests、PresentationFxPoolTests、
PresentationAudioTests 和 G4 stress 路径应被扩展，而非复制出第二套测试框架。

### 7.2 Player 证据包

从干净候选构建 Windows x64 Player 后，输出至：

    ui-concepts/unity-validation/v0.4.0-battle-showcase/<commit>/

证据包必须包含：

- 完整 EditMode、PlayMode XML 和日志；
- 1920×1080、1920×1200 各一组 7 张命名截图，对应第 2.1 节检查点；
- showcase-1x.mp4：45–60 秒完整演示；
- showcase-2x.mp4：同一事件序列的 2× 演示；
- showcase-skip.mp4：至少覆盖攻击中断、亡语链中断和最终无残留状态；
- 构建清单、Git 提交、Unity 版本、配置哈希、截图/视频 SHA-256；
- 素材清单、Prompt/源文件和音频来源清单。

视频录制本身不是领域真相来源；领域一致性仍由测试与 G4 检查点验证。视频用于审看
节奏、可读性、遮挡和主观打击感。

### 7.3 人工审看脚本

评审者不看战斗日志，按顺序回答：

1. 谁攻击谁？这次是轻击、重击还是斩杀？
2. 这次是否被护盾格挡？护盾何时破裂？
3. 战吼来自哪一个单位，改变了哪个目标？
4. 哪个单位死亡后触发了亡语？它召唤了什么？
5. 溅射从哪个主目标扩散到哪些次目标？
6. 在 1× 下是否存在“等待动画”的明显烦躁点？

三名内部观察者中至少两名正确回答前五项，且没有共同 P0/P1 遮挡、错序、误导或残留
问题，Showcase 才可作为 FX-C 的候选证据。

## 8. 实施顺序与决策门

| 阶段 | 工作 | 验证 | 退出条件 |
| --- | --- | --- | --- |
| A | 关闭 FX-A 的 Prefab 重建、全量测试和基础攻击对比 | 当前 UGUI 图形池、1×/2×/Skip | 普通攻击已有明确接触感 |
| B | 建立真实 Showcase fixture 与检查点，不导入新资源 | PlayMode 事件顺序和池清理 | 四段均可稳定重放 |
| C | 生成/筛选最小 VFX 与音频候选，建立来源清单 | 导入、Alpha、Catalog、Cue 校验 | 14 个 VFX 槽位和 10 个 Cue 候选完整 |
| D | 接入 FX-B 元数据、关键词/效果配方与材质 | EditMode、PlayMode、压力回归 | 1×/2×/Skip 结果一致、无残留 |
| E | 构建 Player，录制证据并进行三人审看 | 双分辨率、视频、人工问答 | 可决定进入 FX-C 扩展或回到事件/时序修正 |

任何阶段出现“看不出谁对谁做了什么”的问题时，优先修正事件元数据、时序、目标连线和
遮挡，不能通过追加说明文字或更多粒子掩盖。

## 9. 风险与控制

| 风险 | 控制措施 |
| --- | --- |
| FX-A 尚未完成 Unity 复验 | 先在有效 Unity 2022.3.62f3c1 环境关闭 FX-A；Showcase 不提前宣告通过 |
| 资产生成风格漂移 | 唯一 Style Tile、透明底硬约束、同一批次评审与源文件/哈希记录 |
| 亡语/召唤链视觉拥挤 | 串行 40ms 以上间隔；低优先级尘雾可降级，主事件绝不丢失 |
| 视觉层篡改规则理解 | 元数据只读；模拟结果与哈希在 1×/2×/Skip 下必须相同 |
| 音频过载或缺少许可 | 候选音频限并发并标 Commissioning；正式 ProductionStrict 保持独立门禁 |
| 项目范围膨胀 | 只生成 14 个 VFX 槽位，三流派先 Tint，不做单卡专属效果 |

## 10. 评审后的下一步

本方案获准后，第一项实现工作不是立即生成全部素材，而是：

1. 在有有效 Unity 许可的环境中关闭 FX-A；
2. 建立四段真实 Showcase fixture 与自动化检查点；
3. 先以当前 UGUI 形状校准时序；
4. 再生成并筛选第 4.2 节的最小候选素材包；
5. 以 Player 视频和三人审看结果决定是否进入 FX-B 全量关键词/法术扩展。

这样能避免在事件映射和节奏尚未成立前，过早生产无法复用的美术与音频资产。
