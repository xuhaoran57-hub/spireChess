# 项目待办

版本：0.1
当前目标：R17 Phase 6B 的旧 S1/S2 继续暂缓；阶段 9A 已完成并建立候选；阶段 9B G0 已通过，当前只推进 G1 Style Tile 冻结，以纯“旅团绘本（Wandering Storybook）”v0.3 完成视觉、可读性、流派区分、金色表现和批量生产成本评审，G1 通过前不进入批量资产生产。

## 已完成：阶段 0 项目准备

- [x] 创建 Unity 2022 LTS 2D 项目。
- [x] 建立 Unity 目录结构：
  - `Assets/Resources/Configs/Json/`
  - `Assets/Scripts/`
  - `Assets/Scenes/`
  - `Assets/Prefabs/`
  - `Assets/Art/Placeholders/`
- [x] 接入 Newtonsoft Json。
- [x] 将以下配置放入 Unity：
  - `minions.v0.1.json`
  - `spells.v0.1.json`
- [x] 实现 `ConfigService`，读取随从和法术 JSON。
- [x] 实现配置校验：
  - 随从 ID 不重复。
  - 法术 ID 不重复。
  - 随从数量为 52，其中 Token 为 2。
  - 法术数量为 16，其中 15 张普通法术、1 张三连发现系统法术。
  - `tier`、`race`、`keywords` 合法。
- [x] 实现最小存档接口：
  - `ISaveStorage`
  - `FileSaveStorage`
  - 使用 `Application.persistentDataPath`
- [x] 创建并加入 Build Settings：
  - `Boot`
  - `MainMenu`
  - `BattleTest`
  - `ShopTest`
  - `RunTest`
- [x] 实现 Play 后自动进入 `BattleTest` 的启动链路。

## 已完成：阶段 1 战斗原型基础

- [x] 创建 `BattleTest` 场景。
- [x] 从 `MinionConfig` 生成运行时卡牌 UI。
- [x] 显示双方各 5 个战斗格。
- [x] 支持拖拽调整同阵营站位。
- [x] 实现运行时随从对象：
  - 当前攻击
  - 当前生命
  - 永久攻击加成
  - 永久生命加成
  - 护盾状态
  - 是否金色
- [x] 实现最小自动战斗：
  - 从左到右行动。
  - 普通攻击。
  - 嘲讽优先。
  - 护盾抵挡伤害。
  - 死亡移除。
  - 胜负判断。
- [x] 实现战斗日志。
- [x] 将瞬间结算改为逐步播放：
  - 每次攻击后刷新卡牌生命、护盾和死亡移除。
  - 每次攻击后增量追加日志。
  - 高亮攻击者和目标槽位。

## P0：已实现，待 Unity 场景验收

- [x] 定稿目标选择规则。
  - 无嘲讽时，从所有存活敌人中等概率随机选择目标。
  - 存在多个嘲讽时，仅从存活嘲讽单位中等概率随机选择目标。
  - 目标选择不考虑对位、距离或左右顺序。
- [x] 实现溅射规则：
  - 普通溅射将本次攻击伤害拆分给目标左右两侧，总和为 100%。
  - 奇数攻击力时，一侧向下取整、另一侧向上取整，多出的 1 点随机分配。
  - 金色溅射对目标左右两侧造成 100% 伤害。
  - 分配先于相邻槽位判定；一侧为空时不会把该侧伤害转移给另一侧。
  - 溅射伤害写入逐步播放和战斗日志。
- [x] 增加战斗测试预设，方便一键切换阵容并反复测试。
- [x] 增加 EditMode 回归测试，覆盖随机目标、随机嘲讽、溅射分配、空侧、金色和护盾。

## P1：阶段 1 收口

- [x] 实现 Token 召唤、召唤失败和立即攻击。
- [x] 实现最小亡语召唤。
- [ ] 实现最小光环规则（暂缓）。
- [x] 提供独立连续模拟 10 场的调试入口，输出玩家/敌方胜场与平局数。
- [x] 记录阶段 1 已确认规则：
  - 普通攻击继续按槽位从左到右、双方交替；本轮新召唤单位也会加入普通行动队列。
  - 亡语召唤优先死亡原位，后续召唤选择距离原位最近的空槽，左右同距时先右后左；满场时失败。
  - 立即攻击作为额外攻击独立结算；若召唤物仍存活，本轮仍按当前位置获得一次普通行动。
  - 护盾抵挡一次任意正数伤害，包括普通攻击、反击和溅射。
- [ ] 定稿光环在死亡、移动、召唤后是否实时刷新（随光环效果暂缓）。

## P1：阶段 2 商店与经济

- [x] 定稿阶段 2 经济与商店规则：
  - 玩家以空阵容和空备战区开局。
  - 每回合金币从 3 递增并重置，最高 10。
  - 出售和资源效果可以使当前金币超过 10，超额部分不跨回合保留。
  - 刷新 1、随从 3、法术 1、出售随从返还 1。
  - 酒馆升级费用 5 / 7 / 9 / 10，未升级时下回合降低 1。
  - 随从位按酒馆等级为 2 / 2 / 3 / 3 / 4，固定 1 个法术位。
  - 随从与法术共同占用 5 格备战区，满格时禁止购买。
  - 法术不能出售或丢弃，只有合法使用后释放格子。
  - 第一版支持免费整店冻结和局内有限随从牌池。
  - 随从按剩余实体副本等概率抽取，法术按合法配置等概率抽取。
  - 只有主动刷新计入刷新次数；阶段 2 建立商店事件入口但不实现全部卡牌效果。
- [x] 实现商店刷新。
- [x] 实现购买随从和法术。
- [x] 实现出售随从。
- [x] 实现金币与回合经济。
- [x] 实现酒馆升级。
- [x] 实现共享备战区和随从上下场。
- [x] 实现整店冻结、有限随从牌池和商店事件入口。
- [x] 增加商店与经济 EditMode 回归测试。

## P1：阶段 3 构筑兴奋点

- [x] 定稿阶段 3 第一批规则：
  - 战吼绑定 `OnPlay`，仅从备战区进入空战斗位时触发；战斗区随从不能手动回收。
  - 三连或明确效果使随从返回备战区后，再次上场可以重新触发战吼。
  - 无合法目标的战吼不阻止随从上场，法术仍禁止零收益使用。
  - 战吼和法术都只选择最终战斗区；“友方随从”不包含战吼来源自身。
  - 只有战斗区随从可以在商店阶段出售，备战区未打出随从不能出售。
  - 战斗区与备战区的三个同名普通随从自动三连。
  - 金色随从统一进入备战区，继承永久攻击、生命和永久关键词，不继承护盾消耗、下一战增益和触发计数。
  - 三连不直接生成奖励；三连生成的金色随从首次打出时赠送一张发现法术到备战区，每个实例只赠送一次。
  - 发现法术只发现使用时当前酒馆等级的随从，并按剩余实体副本等概率抽取。
  - 发现为阻塞流程；三连发现必须选择一个候选，不能通过取消重新抽取；其他明确允许取消的效果选择在完成或取消前同样阻塞商店操作。
  - 商店阶段同时承担阵容准备功能，暂不建立独立战前阶段。
- [x] 实现持有随从实例状态和战斗快照 `SourceInstanceId` 映射。
- [x] 实现 `OnPlay` 战吼触发。
- [x] 收紧阵容移动：禁止战斗区随从手动回收及占位交换回收。
- [x] 收紧出售：移除备战区出售，只允许商店阶段出售战斗区随从。
- [x] 实现统一效果验证与执行入口。
- [x] 扩展目标配置：最终战斗区与是否包含战吼来源。
- [x] 固定玩法响应、三连检测和领域通知的结算顺序。
- [x] 接入观星学徒和黑市小贩战吼。
- [x] 实现金色三连合成。
- [x] 实现金色随从首次打出时的一次性发现法术奖励。
- [x] 实现阻塞式发现流程与候选牌池保留/返还。
- [x] 增加奖励法术 `shopEligible=false` 配置能力。
- [x] 将阶段 3 法术配置的 `useTiming` 收敛为 `Shop`，并统一配置价格为 1。
- [x] 接入小型锻体、精准训练、厚皮药剂和免费刷新。
- [x] 增加阶段 3A EditMode 回归测试，并通过纯 C# 测试桩验证。
- [x] 增加阶段 3B EditMode 回归测试，覆盖三连顺序、延迟合成、金色继承、一次性奖励、阻塞与牌池返还，并通过纯 C# 测试桩验证。

## P0：Unity 迁移与集成验收

- [ ] 在目标机器拉取最新提交，使用 `sc/ProjectSettings/ProjectVersion.txt` 指定的 Unity 版本打开 `sc` 项目。
- [ ] 等待首次资源导入和脚本编译完成，确认所有 `.meta` 与 `.asmdef` 正常识别。
- [ ] 确认 `Unity.Newtonsoft.Json` 和 Unity Test Framework 依赖可用，Console 达到 0 个编译错误。
- [x] 在 Unity Test Runner 运行全部 EditMode 测试，当前自动化 81 / 81 通过。
- [x] 验证配置加载：52 个随从、16 张法术；阶段 5 开放 15 张普通商店法术并保留 1 张三连发现系统法术。
- [ ] 手动复核阶段 1 战斗表现：
  - 无嘲讽和多个嘲讽时的随机目标。
  - 普通奇数攻击溅射、空侧处理、金色溅射和护盾。
  - Token 从死亡原位向附近召唤、立即攻击及本轮正常攻击。
  - 战斗快照不会因战斗死亡或临时状态修改玩家持有实例。
- [x] 建立商店场景或控制器，只调用 `ShopSession` 领域接口，不在 MonoBehaviour 中重复实现规则。
- [x] 接入商店基础 UI：金币、酒馆等级、升级费用、刷新次数、随从位、法术位和冻结状态。
- [x] 接入构筑交互：
  - 购买、刷新、升级、冻结和出售战斗区随从。
  - 备战区随从拖入空战斗位、战斗区内部换位并禁止手动回收。
  - 法术选择最终战斗区目标，失败或零收益时保留法术并显示原因。
  - 再次点击已选法术可以取消选择，顶部显示剩余免费刷新次数。
  - 结束商店后锁定操作并从最终战斗区生成战斗快照。
- [x] 接入三连与发现表现：
  - `OnTripleFormed` 播放三连反馈并刷新战斗区、备战区。
  - `OnTripleRewardGranted` 显示发现法术进入备战区。
  - `OnDiscoverStarted` 打开阻塞式候选界面，候选读取 `PendingDiscover.Candidates`。
  - 三连发现选择调用 `SelectDiscover(index)`，不提供取消；其他可取消效果选择使用统一选择接口。
  - 发现结束前禁用购买、出售、移动、法术、刷新、升级、冻结和结束商店。
- [x] 增加最小 PlayMode 集成测试：购买进入备战区、上场触发战吼、三连刷新 UI、发现阻塞、结束商店进入战斗。
  - 自动化结果：EditMode 52 / 52、PlayMode 3 / 3 通过。
- [x] 检查场景重载和重复进入商店时不会重复注册领域事件。
- [x] 按 `phase-2-4-unity-acceptance-checklist.md` 完成阶段 2-4 Unity 人工验收并更新结果。

## P0：阶段 4A 单层最薄闭环

- [x] 定稿阶段 4 地图、节点、生命、奖励和流程边界，见 `phase-4-node-design-v0.1.md`。
- [x] 完成阶段 4A 技术方案，见 `phase-4a-technical-design-v0.1.md`。
- [x] 实现 `RunState`、`RunPhase`、节点尝试和结算幂等。
- [x] 实现固定单层地图、遭遇/奖励配置及校验。
- [x] 将商店经济时钟改为显式使用 `RunTurn`，并保持旧测试入口兼容。
- [x] 区分正常胜负、双方同时倒下和回合上限三种战斗结果原因。
- [x] 实现玩家生命、伤害结算、延迟资源和 FIFO 卡牌奖励队列。
- [x] 接入 `RunTest → ShopTest → BattleTest → RunTest` 场景闭环。
- [x] 增加 4A EditMode、PlayMode 测试并完成全量自动化回归。
  - 自动化结果：EditMode 60 / 60、PlayMode 6 / 6 通过。

## P0：阶段 4B 路线风险收益

- [x] 完成阶段 4B 技术方案，见 `phase-4b-technical-design-v0.1.md`。
- [x] 泛化节点尝试、选择阶段和确定性随机流。
- [x] 扩展为 7 节点固定地图并接入精英、事件、锻造和恢复配置。
- [x] 实现阻塞式精英三选一及候选牌池预留生命周期。
- [x] 实现持有随从永久修改入口、三个锻造配方和目标型奖励。
- [x] 实现五个事件、原子效果事务和危险招募后续奖励。
- [x] 实现恢复点三个选项和非战斗节点经济推进。
- [x] 扩展 `RunTest` 地图预览与奖励/事件/锻造/恢复覆盖层。
- [x] 增加 4B EditMode、PlayMode 测试并完成全量自动化回归。
  - 自动化结果：EditMode 69 / 69、PlayMode 9 / 9 通过。

## P0：阶段 4C 三层完整单局

- [x] 完成阶段 4C 技术方案，见 `phase-4c-technical-design-v0.1.md`。
- [x] 扩展第一至第三层固定地图和逐层递增的普通、精英、Boss 遭遇。
- [x] 新增 `FloorComplete` 阶段和显式跨层推进，保留阵容、生命、经济、酒馆等级、牌池与奖励队列。
- [x] 正式启用第一、二层 Boss 三选一和动态“当前酒馆等级 +1”随从奖励。
- [x] 实现第三层 Boss 最终通关、战斗/精英/Boss/三连统计和最小结算展示。
- [x] 扩展 `RunTest` 切层入口、地图重建、累计战绩和最终结算信息。
- [x] 增加 4C EditMode、PlayMode 测试并完成全量自动化回归。
  - 自动化结果：EditMode 73 / 73、PlayMode 11 / 11 通过。
- [x] 根据阶段 2-4 人工验收收紧可用内容：仅 4 张已实现法术进入商店/奖励池，未接入效果的随从显示基础身材原型提示。
- [x] 运行收紧内容后的全量自动化回归：EditMode 73 / 73、PlayMode 13 / 13 通过。

## P0：阶段 4D 显式商店地图

- [x] 完成显式商店地图技术方案，见 `phase-4d-explicit-shop-map-technical-design-v0.1.md`。
- [x] 增加 `Shop` 地图节点，将经济回合收敛为 `ShopTurn`，另以 `MapStep` 记录地图访问。
- [x] 重建三层地图：每条路线固定 5 个商店和 5 场战斗，功能分支不改变经济次数。
- [x] 战斗节点直接进入战斗；事件、锻造、恢复不再推进商店经济。
- [x] Boss 重试不再开店、发金币或推进回合，并固定同节点战斗随机流；失败尝试不提交永久成长和战后奖励。
- [x] 新增 21 个正式遭遇的渐进身材曲线，第三层最终 Boss 开战前原始总身材为 100/100。
- [x] 根据首次完整单局反馈降低第一、二层难度：第一层改为 1/2 → 11/23，第二层改为 14/18 → 36/40；减少普通战满编与金色单位，精英/Boss 失败伤害减半，第三层保持不变。
- [x] 修复随从发现自循环：随从触发的发现会在牌池预留前排除来源卡 ID，命运洗牌师刷新 3 次后不再能发现自己；正式卡牌增加高对比金边、金色卡名和常驻“金色”徽章。
- [x] 修复战斗大数值显示：攻击/生命文本按冻结区域自动缩小字号，覆盖三位数和 `999/1200`，短数值重新渲染时恢复标准字号。
- [x] 更新 `RunTest` 商店节点、战斗序号、商店回合/地图步数和 11 列紧凑布局。
- [x] 完成自动化回归：EditMode 185 / 185、PlayMode 18 / 18。
- [x] 在 Unity 中完成人工地图、完整三层单局和 Boss 失败重试验收（2026-07-21 完成）。
- [ ] 提交新运行时候选后重跑 S1，并从种子 2000 重新开始 S2；旧 5.1.0 数据只作改造前证据。（暂缓，等待后续卡牌调整收敛并重新建立候选。）

## P0：阶段 5A 通用效果系统

- [x] 完成阶段 5A 技术方案，见 `phase-5a-effect-system-technical-design-v0.1.md`。
- [x] 建立共享效果定义、目标解析、条件判断和次数限制。
- [x] 将商店效果迁移到 `ShopEffectEngine`，并泛化阻塞式选择。
- [x] 建立战斗 FIFO 触发队列，接入战斗开始、护盾、召唤、死亡和战斗结束事件。
- [x] 实现 `NextCombat` 一次性状态、战后存活增益与战斗永久差量回写。
- [x] 增加配置组合校验、2048 事件循环保护、确定性批量模拟和自动化测试。

## P1：阶段 5B 三流派低等级闭环

- [x] 完成阶段 5B 技术方案，见 `phase-5b-archetype-content-technical-design-v0.1.md`。
- [x] 增加 `implementationStatus` 内容状态并区分原型、正式可用和禁用内容。
- [x] 5B-1：完成 10 个 1 级随从和 3 张 1 级法术。
- [x] 5B-2：完成 10 个 2 级随从和 3 张 2 级法术。
- [x] 5B-3：完成 10 个 3 级随从和 3 张 3 级法术。
- [ ] 为铸魂、荒灵、星契分别建立固定构筑与完整三层冒烟测试。
- [x] 恢复已完成功能卡牌的真实文案并从正式内容池开放。

## P1：阶段 5C 完整内容池与平衡

- [x] 完成阶段 5C 技术方案，见 `phase-5c-content-pool-balance-technical-design-v0.1.md`。
- [x] 增加版本化发布清单和跨资源内容校验。
- [x] 完成 4-5 级 20 个随从与 6 张法术。
- [x] 扩充到 7 套普通遭遇、3 个精英、3 个 Boss 和 10 个事件。
- [x] 完善普通、精英、Boss 和事件奖励池。
- [x] 建立本地 NDJSON 遥测、种子摘要和固定构筑 100 场批量模拟工具；完整命令重放延后到阶段 6。
- [x] 战斗日志改为完整保留并支持纵向滚动回看，不再截断为最后 32 行。
- [x] 第 2、3 层各增加两个必经普通战斗节点，并让地图布局自适应五列节点。
- [x] 为 T1-T5 卡牌建立灰、绿、蓝、紫、橙五档背景色，统一应用于商店和战斗界面。
- [x] 阶段 5 自动化基线曾达到 EditMode 83 / 83、PlayMode 14 / 14；v0.2 扩充后为 EditMode 93 / 93、PlayMode 14 / 14。
- [ ] 完成至少两轮数值调整与阶段 5C Unity 人工验收（按当前决策暂缓，待 v0.2 机制联合验收后再恢复）。

## P1：随从池 v0.2 扩充与原卡再平衡

- [x] 完成 v0.2 设计稿，覆盖 64 张非 Token 目标池、14 张新增随从以及铸魂/荒灵原有随从改造，见 `minion-pool-expansion-rebalance-design-v0.2.md`。
- [x] 按 v0.2 完整实现铸魂、荒灵原有随从改造和金色终结器上限；R1 将普通/金色不熄炉王上限进一步调整为 2/4。
- [x] 在当前 64 张非 Token 池中重跑六套固定构筑，记录受限永久成长与战斗临时成长预算。
- [x] 新增 9 张主种族随从，使三个主种族各等级数量达到 `3 / 4 / 4 / 4 / 3`。
- [x] 新增双尾狐影 Token，实现狐群巢母的嵌套亡语、逐个召唤和召唤物身份隔离；Token 不触发非 Token 死亡成长且不回写永久差量。
- [x] 为每次成功召唤记录事件标识，并按“召唤事件 + 单位实例”去重立即攻击；同一单位因同一次召唤最多立即攻击一次，多个来源不叠加。
- [x] 新增 5 张旅团随从，使旅团每等级达到 2 张。
- [x] 实现百技学徒的相邻战斗关键词临时复制，以及敌方 Token 召唤观察、第 N 次触发、多目标下一战强化和带上限的差值效果。
- [x] v0.2 新卡不再配置 `archetypes`；旧卡字段仍仅作历史分析元数据，后续随遥测 schema 迁移统一移除。
- [x] 同步版本发布清单、奖励池、遭遇池、根目录镜像和跨资源内容校验。
- [x] 按 `phase-5-unity-acceptance-checklist.md` 完成 v0.2 Unity 功能联合人工验收。
- [ ] 重跑六套构筑的正常/最高发育并统计牌池三连率；正式数值批次按当前决策暂缓。

### 切换 Unity 后的实施清单

- [x] 改动 v0.2 前运行全量基线：EditMode 83 / 83、PlayMode 14 / 14。
- [x] P0：落地本轮战斗规则。
  - [x] R1 调整为普通不熄炉王每场最多转移护盾 2 次，金色最多 4 次。
  - [x] 每次成功召唤生成独立事件标识，按“召唤事件 + 被召唤单位实例”去重 `ImmediateAttack`。
  - [x] 迅捷幼灵与万蹄奔潮同时生效时只立即攻击一次；若仍存活，本轮仍能正常行动。
  - [x] 没有立即攻击的多单位召唤继续逐个生成，全部生成后进入正常行动队列。
- [x] P0：落地狐群巢母和双尾狐影。
  - [x] R1 调整为狐群巢母普通/金色身材 4/7、8/14。
  - [x] 普通链为 `2/2 → 两个 1/1`，金色链为 `4/4 → 两个 2/2`。
  - [x] 双尾狐影不消耗牌池、不触发非 Token 死亡成长、不持有 `SourceInstanceId`，战后不回写永久差量。
  - [x] 覆盖单格复用、逐个召唤和第二个幼灵召唤失败。
- [x] P0：同步并校验内容配置。
  - [x] 根目录与 Resources 的 `minions.v0.1.json` 完全一致，并有自动化镜像校验。
  - [x] 校验 64 个非 Token、3 个 Token、总计 67 条配置及等级/种族分布。
  - [x] 新增普通/金色文案与效果同步，新卡不配置 `archetypes`。
- [x] P0：补充 EditMode 回归测试，v0.2 完成后为 93 / 93。
- [x] P1：规则测试通过后批量加入其余 v0.2 内容。
  - [x] 完成铸魂、荒灵原卡改造及新增主种族与旅团随从。
  - [x] 完成关键词复制、敌方 Token 观察、第 N 次触发、多目标下一战强化和带上限差值效果。
  - [x] 更新发布清单、奖励池与遭遇池，完整三层 PlayMode 回归 14 / 14 通过。
- [x] 用新规则重跑六套固定阵容，记录正常/全金首轮伤害、召唤失败数和立即攻击次数；D01 最终存活仍全为 0，核心时点存活率留待遥测补充。

## P0：阶段 6 统计与遥测基础设施

- [x] 以 Resources 为配置真源同步根目录法术文件，并增加法术精确镜像测试。
- [x] 建立 12 个 `N/H` 构筑快照、输出木桩、两档机制压力板和 84 个正式场景矩阵。
- [x] 增加战斗语义诊断、逐场确定性哈希、镜像聚合、CSV 输出和规范化 `configHash`。
- [x] 增加完整单局事件、商店/回合 10 快照、核心分类、RunEnded、单局汇总和卡牌漏斗。
- [x] 增加人工决策摘要模板及 S0 逐场确定性测试。
- [x] 完成 `R0-preflight-dotnet`：84,000 场 S1、8,400 场 S0 复跑；确定性失败、异常和效果上限均为 0，结果见 `balance-results/phase-6-v0.2/R0-preflight-dotnet/`。
- [x] 完成 R1 六个单轴 S0 与合并预演：84,000 场 S1、8,400 场确定性复跑；竞技安全告警为 0，结果见 `balance-results/phase-6-v0.2/R1-combined-dotnet/`。
- [x] 在 Unity 2022.3.62f3c1 Test Runner 完成新增后的全量回归：EditMode 121 / 121、PlayMode 14 / 14。
- [x] 增加 `SpireChess.Editor.BalanceBatchCommand` 正式批次入口，输出逐场压缩样本、场景汇总、镜像汇总、确定性差异和版本化 metadata。
- [x] 完成 `R1-flourish-unity`：S0 8,400 场首跑与 8,400 场同种子复跑、S1 84,000 场；确定性失败、异常、效果上限和竞技回合上限均为 0。
- [x] 完成 `R2-shield-source-consolidation` S0：移除三张中低级铸魂的战中补盾，8,400 场首跑与 8,400 场复跑全绿；B01 N/H 等权得分率降至 50.70%/73.30%。
- [x] 使用当前 R2 配置重跑六流派 S0 并生成逐流派分析：B02 全档偏强，B03-N/B04-H/B06-N 偏弱，B05-N 首轮输出仍需复核。
- [x] 完成 `R3-B02-B04-trigger-conversion` S0，并增加第 10 回合流派/经济快照、P50/P90 成长校准、fixture v0.3 独立 N/H 字段和离线聚合入口。
- [x] 使用统一五回合成长模拟生成 provisional `balance-fixtures.v0.3.json`，完成 `R4-five-turn-growth-v0.3-dotnet` S0：8,400 场首跑与 8,400 场复跑安全门禁全绿；25/30 组对局越过 25%-75% 线，暂不冻结。
- [x] 完成无上限玩家级繁茂、百鸣先增层再召唤、熔核有效授盾永久成长、裂甲战后随机铸魂奖励及其余确认随从更新；`R7-minion-update-unbounded-flourish-dotnet` 完成 8,400 场首跑和 8,400 场复跑，安全门禁全绿。
- [x] 使用最新六套阵容完成三回合亡语永久成长校准并更新 fixture v0.3；`R8-best-lineups-deathrattle3-dotnet` 完成 8,400 场首跑和 8,400 场复跑，确定性失败、异常、效果上限和竞技回合上限均为 0。
- [x] 完成藤冠祭司 4/2 死亡阈值、秘页折光师护盾与前两次施法永久成长、B02/B05 换阵和核心分类器 0.2.2；使用 Unity 运行五回合、每档 1,000 样本成长并重算 fixture v0.3，`R9-lineup-growth-unity` S0 的 8,400 场首跑和复跑安全门禁全绿。
- [x] 天穹契约者改为每 4 次刷新重复触发群体永久成长，金色万蹄奔潮移除召唤物溅射；重算 B06 五回合 P50 后 N 72/89、H 128/157 均未变化，`R10-sky-hoof-unity` S0 安全门禁全绿。
- [x] 将 B06 五回合成长改为自然经济尽量刷新、法术完全依赖刷新赋予；每档 1,000 样本的 P50 更新为 N 60/72、H 133/157，并同步 fixture v0.3。
- [x] 将 B06 刷新预算修正为每回合基础 10 金币，效果额外金币继续叠加；每档 1,000 样本的 P50 更新为 N 88/100、H 215/239，并同步 fixture v0.3。
- [x] 完成 R13 金色百鸣与天穹三目标：12,000 个成长样本更新 B03/B06 fixture；.NET S0 8,400 场首跑与 8,400 场复跑安全门全绿。
- [x] 完成 R14 天穹非对称刷新：普通每 4 次、金色每 3 次刷新后使所有友方星契永久 +1/+1；12,000 个成长样本将 B06 fixture 更新为 N 88/100、H 165/189，.NET S0 8,400 场首跑与同种子复跑安全门全绿，B06-N/H 为 62.00%/59.45%。
- [x] 完成 R15 四异常档位联调：12,000 个五回合成长样本更新全部 fixture；.NET S0 8,400 场首跑与同种子复跑安全门全绿，25%-75% 外对局由 13/30 降至 9/30。
- [x] 完成 R16 千环与星盘调整：普通千环守墓者亡语护盾改为两个随机存活友军，星盘校准师改为每个商店阶段第一次刷新后使最低攻击友方星契永久 +1 攻击、金色 +2 攻击；五回合夹具更新 B01-N 60/59、B06-N 88/95、B06-H 165/179；.NET S0 8,400 场首跑与同种子复跑安全门全绿，B06-N/H 为 49.30%/59.40%。
- [x] 完成 R16 工程基线冻结：Unity 2022.3.62f3c1 EditMode 128 / 128、PlayMode 14 / 14；同配置 S0 的 8,400 场首跑与 8,400 场复跑安全门全绿；Unity/.NET 逐场确定性哈希 8,400 / 8,400 一致。当前配置和 fixture v0.3 冻结供阶段 7 使用，完整记录见 `balance-results/phase-6-v0.3/R16-tomb-astrolabe-tuning-unity/R16-engineering-baseline-freeze-report.md`。
- [x] 完成内容 5.3.1 的 2 级荒灵改造：疾羽林隼改为 2/4 并移除开场立即攻击；腐叶承嗣改为嘲讽亡语，普通/金色随机使一个存活的其他非召唤物友方荒灵本场 +2/+2、+4/+4；同步配置镜像、图鉴和第二层开场 14/18 遭遇曲线，Unity EditMode 197 / 197、PlayMode 18 / 18 通过。
- [x] 完成内容 5.3.1 Unity 增量人工验收：疾羽林隼、腐叶承嗣普通/金色行为与文案一致；金色星图掮客、旧塔向导和星门讲师的两轮发现、第二轮弹窗、手牌占位和全局阻塞均无问题。
- [x] 完成 `R17-content-5.3.1-unity` 独立 S0：基线提交 `76a0640dd69c`、配置哈希 `df8034664c76abe03a9aa9ad024da7ebcb57f9addcf5948e53308e8c50b6622c`；8,400 场首跑与 8,400 场同种子复跑的确定性失败、异常、效果上限和竞技回合上限均为 0，结果见 `balance-results/phase-6-v0.3/R17-content-5.3.1-unity/`。
- [ ] Phase 6B：正式 UI 可用后，完成当前冻结候选的 84,000 场 S1、20 局人工单局和未查看留出集复核。（暂缓，等待后续卡牌调整收敛并重新建立候选。）
  - [x] 完成改造前 5.1.0 R16 Unity `S1_CALIBRATION`：种子 1000-1999、84 个场景、84,000 场；全部工程安全门通过。结果保留在 `balance-results/phase-6-v0.3/R16-tomb-astrolabe-tuning-unity-phase-6b/`，不与显式商店地图 5.2.2 的 S2 合并。
  - [ ] 使用内容 5.3.1 的提交 `76a0640dd69c` 和配置哈希 `df8034664c76abe03a9aa9ad024da7ebcb57f9addcf5948e53308e8c50b6622c` 运行新的 `S1_CALIBRATION`；不得与 5.2.2、5.3.0 或 R16 结果混用。（暂缓；若后续调整卡牌，必须改用新候选身份，不再补跑此旧提交。）
- [ ] Phase 6B：使用种子 2000-2019 完成人工单局，记录流派形成、三连、路线选择、时长和受影响机制体验。（暂缓，不使用当前 R17 S0 直接开启 S2。）
  - 种子 2000 的 5.1.0 原始记录作为 Boss 重试刷经济的问题证据保留；5.3.1 从同一种子重新开始，不覆盖 5.1.0/5.2.2/5.3.0 文件。
- [ ] Phase 6B：每个流派补足至少 10 个第 10 回合可识别成型人工样本；若实机数据要求解冻，基于冻结 manifest 新建候选，不覆盖 R16 基线。（随 S1/S2 一并暂缓。）

## P0：阶段 7 UI 纵向切片

- [x] 完成正式商店 UI 技术实施方案，见 `phase-7-ui-vertical-slice-technical-design-v0.1.md`。
- [x] 完成天穹契约者完整卡面 v0.1 概念稿，见 `ui-concepts/card-ui-sky-covenant-v0.1.svg` 和 PNG 预览。
- [x] 验证 AI 位图生成流程并保留第一版独立探索稿，见 `ui-concepts/card-ui-generated-initial-draft-v0.1.png`。
- [x] 确认完整随从卡 UI v0.2 的视觉语言和信息骨架：
  - 普通天穹契约者验证银黑公共框架、星契种族皮肤、两项能力标签和可选刷新进度，见 `ui-concepts/card-ui-sky-covenant-normal-v0.2.png`；
  - 金色天穹契约者验证金色框架、流光和高稀有度表现，见 `ui-concepts/card-ui-sky-covenant-golden-style-v0.2.png`；该图只作为金色风格参考，正式数据必须使用 8/16、每 3 次刷新和对应的三段进度；
  - 万蹄奔潮验证荒灵种族皮肤、三项能力标签、无进度模块和 64 字长文案，见 `ui-concepts/card-ui-ten-thousand-hoof-normal-v0.2.png`。
- [x] 补齐卡牌 UI 契约：固定 `2:3` 比例与完整/紧凑模式几何，定义随从/法术变体、长文案截断，以及普通、金色、成长、永久护盾、下场护盾、临时、选中、合法目标和不可操作状态的叠加优先级，见 `phase-7-ui-vertical-slice-technical-design-v0.1.md` 第 7.3 节。
- [x] 实现 `CardViewModel`、`ShopCardViewModelFactory` 和 6 个对应 EditMode 测试，覆盖普通/金色、商品/持有、随从/法术、成长、永久/下场护盾、临时状态、固定费用、中文标签和空参数。
- [x] 冻结正式商店低保真线框图 v0.1，规范副本见 `ui-concepts/shop-ui-wireframe-v0.1.png`；确认顶部状态栏、商品行、战斗区、手牌区和右侧操作/详情栏的信息架构。
- [x] 定义 `ShopScreenState`、5 格/未来 10 格手牌分页、动态按钮和卡牌详情面板状态，并补充 5 个纯 EditMode 测试；UI 使用 `Hand` 命名，领域层暂时保留 `Bench`。
- [x] 实现 `ShopScreenStateBuilder` 和 7 个真实领域对象驱动的 EditMode 测试，映射经济、商品、战斗/手牌、分页、选择、详情状态、免费/付费刷新、冻结、满级、出售、满手牌与发现全局阻塞。
- [x] 实现只读 `ShopTargetingQuery` 并接入 Builder，复用 `ShopEffectEngine` 目标语义映射 `IsLegalTarget`、非法目标和无目标原因；补充 8 个 EditMode 测试，覆盖定向法术、种族/金色/Token 过滤、战吼、无目标战吼例外、模态选择隔离、全局阻塞、领域状态与 RNG 不变。
- [x] 实现纯 C# `UiTextFormatter` 和 10 个 EditMode 测试，覆盖空白/显式换行、单行文本、3/2 能力标签、描述行数、代理对与组合字符安全截断、完整/紧凑模式和异常布局；内容复核将完整随从真实上限由 64 修正为 66（金色 `old_tower_guide`），法术保持 45，待 Unity 下复验几何。
- [x] 整理 `PF_Card` Unity 落地任务单与逐项验收矩阵，见 `pf-card-unity-implementation-checklist-v0.1.md`；覆盖进入门、Prefab 层级、两套几何、TextGenerator、四变体、状态优先级、自动化、两分辨率截图和 DoD。
- [x] 制作共享 `PF_Card`，支持完整/紧凑模式及普通、金色、成长、护盾、下场护盾、临时和目标状态。
  - 固定使用 Noto Sans CJK SC Regular，并随项目保留 SIL OFL 1.1 许可证。
  - 66 字随从、64 字万蹄奔潮和 45 字法术均通过实际字体 Full 文案门；Full 描述区实测扩展为 `12, 256, 216, 52/31`。
  - 新增真实 Prefab Layout/Render 测试，全量结果为 EditMode 173 / 173、PlayMode 14 / 14。
  - 1920×1080、1920×1200 验收截图位于 `ui-concepts/unity-validation/pf-card-v0.1/`。
- [x] 制作静态 `PF_ShopSlot`、`PF_ShopScreen` 和 `PF_ChoiceOverlay`，实现只消费 ViewModel 的 `ShopScreenView` / `ChoiceOverlayView` 渲染骨架。
  - 商品区固定显示 4 个 Full 随从位和 1 个 Full 法术位，战斗区与手牌区各使用 5 个 Compact 槽位；Canvas 固定 1920×1080、Match 0.5、TopBar 96px、ActionRail 220px。
  - 使用布局组完成三行排布，不在运行时代码中计算卡牌坐标；临时状态覆盖五张商品、长文案、金色、选中、护盾、空槽位、详情栏和三选一弹窗。
  - 新增 5 个真实 Prefab 层级、绑定、布局、重复渲染和选择弹窗 EditMode 测试；全量结果为 EditMode 178 / 178、PlayMode 14 / 14。
  - 1920×1080、1920×1200 商店验收图及 1920×1080 选择弹窗验收图位于 `ui-concepts/unity-validation/pf-shop-screen-v0.1/`。
- [x] 将 `ShopScreenStateBuilder` 和正式 View 接入现有 `ShopTestController` / `ShopSession`，保留旧动态 UI 作为迁移期对照。
  - `ShopTest.unity` 默认使用序列化 `PF_ShopScreen` 和 `useLegacyRuntimeUi=false`；程序化创建的 Controller 默认保持 legacy 路径，便于同一套公开操作接口做迁移对照。
  - 正式路径每次操作只在 `ApplyOperation()` 后构建并渲染一次最终状态；领域事件只记录事件日志，不再触发重复 Render。
  - 正式 View 已接入动态按钮、商品/战斗/手牌槽位、选择弹窗和待领取奖励；选择容量由 3 扩展到 4，覆盖四种族效果选择。
- [x] 打通商店购买、出售、刷新、冻结、升级、上阵、换位、发现、奖励处理和进入战斗的正式 UI 调用链。
  - 新增正式场景状态渲染/按钮与卡牌点击回归，以及正式弹窗候选按钮完成强制三连发现回归；既有 RunFlow 同时验证正式奖励弹窗。
  - 当前全量结果为 EditMode 180 / 180、PlayMode 17 / 17；legacy 专用用例继续通过并明确断言使用保留路径。
- [x] 补齐升级、拖拽上阵、战斗位换位和出售的正式 UI 输入级 PlayMode 回归。
  - 按 `BeginDrag → Drop → EndDrag` 真实事件顺序验证手牌上阵与战斗位换位，覆盖正式按钮和卡牌/槽位组件，而非直接调用领域方法。
  - 修复 Drop 触发重渲染后旧拖拽对象返回原槽形成幽灵卡的问题；由目标槽标记已处理拖拽，`EndDrag` 销毁旧视觉实例。
- [x] 实现首版正式商店操作反馈。
  - `ShopScreenView` 只比较同一 `InstanceId` 的前后 ViewModel 快照，显示绿色成长或红色下降浮字，不重新计算领域效果；首次渲染和卡牌跨槽移动不会误播。
  - 新获得永久/下场护盾时放大对应徽章，冻结状态切换时脉冲冻结按钮。
  - Controller 为成功与错误状态提供独立 Toast 语义；Toast 使用非阻塞 CanvasGroup，在运行时停留 1.25 秒后淡出，重复 Render 不会重启同一条消息。
  - 新增卡牌反馈和正式商店状态差值 EditMode 测试；反馈验收图位于 `ui-concepts/unity-validation/pf-shop-screen-v0.1/shop-feedback-1920x1080.png`。
- [x] 修复直接从 `ShopTest` 场景进入 Play 时首回合误显示 legacy UI。
  - `BeforeSceneLoad` 只注册场景回调，回退 Controller 仅在 `sceneLoaded` 后确认场景没有序列化 Controller 时创建，避免场景对象恢复前抢先生成 legacy Canvas。
  - 新增“注册初始化钩子不得立即创建 legacy Controller”和正式场景单 Controller 断言；全量 PlayMode 18 / 18 通过。
- [x] 修复真实 Session 在 1920×1200 下卡牌部分渲染并偏离槽位。
  - `CardView` 的名称、类型和描述适配直接使用 Full/Compact 冻结契约尺寸，不再读取分辨率切换当帧可能过期的 `RectTransform.rect`；`TextGenerator` 的结果按动态字体 `pixelsPerUnit` 还原为逻辑 UI 单位，单行宽度使用 `GetPreferredWidth`，避免 1920×1200、Match 0.5 的约 1.054 Canvas 缩放误判文字溢出。
  - 商店与选择弹窗均先锚定卡牌再执行 Render；新增 1.054 缩放下 80 字名称省略回归，修复前稳定失败、修复后通过。
  - 商店卡牌在 Render 前完成点击/拖拽绑定并隐藏空槽提示，单卡展示异常不再让可见卡牌失去交互。
  - 双分辨率截图工具改为在目标画布尺寸下重新 Render；相关卡牌布局/渲染/商店 Prefab EditMode 17 / 17、全量 PlayMode 18 / 18 通过。
- [x] 完成 1920×1080、1920×1200 正式 Session 场景实机手工验收。
  - 两种分辨率下布局均无核心遮挡或裁切，正常游戏交互通过。
- [x] 确认正式 UI 与 legacy 路径的领域结果一致。
  - 新增两组同配置、同随机种子的 PlayMode 差分回归，在每个操作节点比较结果、经济、商品、牌池、手牌/战斗区完整卡牌状态、候选项、事件数与选择状态。
  - 覆盖刷新、冻结、升级、购买、定向法术、上阵、换位、出售、失败操作、三连发现及发现期间阻塞；操作结束后额外刷新，确认正式 Render / Builder 不推进领域 RNG。
  - 差分专项 2 / 2、全量 PlayMode 20 / 20 通过；奖励领取和商店—战斗—返回流程继续由既有正式输入/跨场景回归覆盖。
- [x] 删除商店 legacy 运行时 UI 搭建路径。
  - 移除 `useLegacyRuntimeUi`、场景回退创建钩子、动态 Canvas/面板/按钮/卡牌/弹窗重建代码及对应序列化写入，`ShopTest` 运行时必须使用场景中的正式 `ShopScreenView`。
  - `InitializeForTests()` 继续允许 headless Controller；原差分用例转为正式 View 与 headless 领域副作用对照，继续检查 Render / Builder 不修改领域状态或 RNG。
  - 正式商店 Prefab EditMode 6 / 6、正式/headless 专项 2 / 2、全量 PlayMode 18 / 18 通过。

## P0：阶段 7B 正式战斗 UI 纵向切片

- [x] 完成阶段 7B 技术方案，见 `phase-7b-battle-ui-technical-design-v0.1.md`。
- [x] 冻结阶段 7B 当前代码边界：结构化播放与展示层基础、正式 View/Controller 接口和可复现 Editor 构建入口纳入本次提交；Prefab、场景、Unity 回归、截图与人工验收留待有 Unity 环境后完成。
- [x] 增加稳定战斗实例 ID 和结构化播放事件；非播放模拟不采集表现事件。
- [x] 实现 `BattleCardViewModelFactory`、`BattleScreenState` 和只读状态构建器。
- [x] 实现 `BattleScreenView`、1×/2× 速度、跳过表现及攻击、伤害、护盾、属性、死亡和召唤反馈。
- [x] 增加 `BattleUiPrefabBuilder`，用于生成 `PF_BattleSlot`、`PF_BattleScreen`、接线 `BattleTest` 和输出双分辨率截图。
- [x] 在 Unity 2022.3.62f3c1 中生成正式 `PF_BattleSlot`、`PF_BattleScreen`、预览场景并完成 `BattleTest` 序列化接线。
- [x] 补齐正式 Prefab EditMode、拖拽/速度输入级 PlayMode 与跳过只结算一次回归；最终全量 EditMode 229 / 229、PlayMode 20 / 20 通过。
- [x] 正式路径验证通过后删除战斗 legacy 动态 UI 回退路径，`BattleTest` 只接受序列化 `BattleScreenView`。
- [x] 完成 1920×1080、1920×1200 截图与静态人工验收；修复 `PF_Card` 在战斗槽内锚点偏移后，两种分辨率均无核心遮挡或裁切。
- [x] 修复 `LogPanel` 覆盖顶栏导致“开始战斗”无法点击：日志面板与顶栏保留 20px 间隔，背景不参与射线，并增加布局与 EventSystem 点击回归；交互式人工复验通过。

## P0：阶段 8A 遗珍系统

- [x] 完成遗珍系统技术方案，见 `phase-8a-relic-system-technical-design-v0.1.md`。
- [x] 定稿初版 15 件遗珍：8 件 Boss 冠冕级、7 件事件奇物级；移除持有数量硬上限，同名遗珍不可重复。
- [x] 完成 15 件遗珍首轮强度复核：增强偏弱的荒野母巢和回收吊钩，将行商零钱袋改为半血追赶效果；B02/B03/B04 列为首轮重点观察项。
- [x] 增加遗珍配置、加载校验、动态持有状态、选择状态、独立确定性随机流和基础遥测。
- [x] 使用冠冕级强制三选一替换第一、二层 Boss 旧功能奖励；接通事件生命交易与奇物级三选一。
- [x] 实现商店费用、周期自动卡牌、续航与战斗快照类遗珍效果。
- [x] 实现亡语/战吼额外触发、阻塞发现次数扩展、效果上限复用和确定性哈希接线。
- [x] 增加 RelicSystemTests，更新受 Boss 奖励替换影响的 EditMode/PlayMode 用例，并通过无 Unity 全量核心编译和专项烟测。
- [x] 完成事件首轮价值调整：危险招募改为失去 3 生命，残缺星图改为失去 2 生命，静谧林地提高到恢复 3 或 2 次免费刷新；升级折扣为零时禁止选择。
- [x] 增加三层各一个专属遇怪事件；玩家可避战或从事件节点直接进入普通战斗，胜负后按事件节点返回地图。
- [x] 建立内容版本 5.4.1、最低规则版本 8A.2；当前完整配置哈希为 87f8a183f30c2a39c640c19f3f16aa5f98ca22ad60aea155fd588c7b3729fc92。
- [x] 在 Unity 2022.3.62f3c1 中完成全量自动化：EditMode 229 / 229、PlayMode 20 / 20。
- [x] 实现正式动态遗珍列表、Boss/事件候选界面与周期进度反馈；接入 `PF_RunScreen` / `RunTest`，删除 Run legacy 动态 UI，并完成 1920×1080、1920×1200 静态验收。
- [x] 完成交互式人工单局中的 Boss/事件遗珍获取、长列表滚动和生命代价反馈验收，并纳入新的正式运行时候选。

## P0：阶段 8B 完整地图与路线预算

- [x] 完成阶段 8B 技术方案，见 `phase-8b-full-map-technical-design-v0.1.md`。
- [x] 冻结首版地图边界：每层 6 个商店、6 场固定战斗、C4 精英、C6 Boss、每条路线 1 个功能节点。
- [x] 冻结固定手工地图优先、未来模板化随机生成共用 `IMapProvider` 与校验器的扩展边界。
- [x] 参数化地图节奏和规则档案，移除领域校验、内容校验、UI 与测试中的 5×5 硬编码。
- [x] 完成三层固定地图配置：每层 19 个可见节点、每条路线 13 个访问节点和 12 条合法 Boss 路线。
- [x] 将常规精英移动到 C4，并新增 24 个正式遭遇，补充、重排三层 C1–C6 遭遇曲线。
- [x] 完成首轮结构路线预算、事件战斗概率、生命价值和遗珍触发次数分析；待 Unity 候选补充实战样本。
- [x] 增加路径枚举、确定性、Boss 重试、事件第 7 战和完整三层无 Unity 测试。
- [x] 建立内容版本 5.5.0、最低规则版本 8B.1；含地图在内的完整配置哈希为 818596be90de4e2ddf6c4b7f9ba0b6e1fee994fcc31ec9893652e02f49ef4311。
- [x] 完成正式横向滚动地图 UI：每层动态渲染 19 个节点与领域连线，显示可达/当前/完成/锁定状态，并在跨层时重建；接入正式节点/选项按钮输入。
- [x] 完成 `PF_RunMapNode`、`PF_RunMapEdge`、`PF_RunRelicEntry`、`PF_RunChoiceOption`、`PF_RunScreen`、`RunUiPreview` 和 `RunTest` 序列化接线；全量 EditMode 229 / 229、PlayMode 20 / 20 通过。
- [x] 完成 1920×1080、1920×1200 地图及 1920×1080 遗珍选择截图检查；响应式主体、横向滚动、连线层级、遗珍长文案和三选一布局无核心裁切。
- [x] 人工完成三条 C4 路线、事件额外战斗和三层流程验收，地图、遗珍与战斗正式 UI 均无阻塞问题；纳入新候选，旧 R17 S1/S2 不继续补跑。

## P0：阶段 9A 正式启动流程与单局存档恢复

- [x] 完成阶段 9A 技术方案，见 `phase-9a-save-resume-technical-design-v0.1.md`。
- [x] 集中生成运行时配置身份，确保内容版本、规则版本和完整配置哈希与平衡门禁共用同一算法。
- [x] 实现保持现有 `System.Random` 输出不变的四条可重放随机流，并补齐 golden/恢复延续测试。
- [x] 定义 v1 存档 DTO、完整领域快照、无副作用恢复构造和领域不变量验证。
- [x] 实现 `.tmp` / 主档 / `.bak` 原子存储、SHA-256 校验、损坏回退和严格版本兼容。
- [x] 实现 `RunPersistenceCoordinator` 和 `SceneFlowRouter`，将 Run/Shop/Battle 成功操作、保存和场景跳转收口。
- [x] 修改 `GameApp`：Boot 进入正式 MainMenu，不再启动即新建单局或运行写盘烟测。
- [x] 制作 `PF_MainMenuScreen`、确认弹窗和 Run/Shop/Battle 共用系统菜单。
- [x] 覆盖全部稳定 `RunPhase` 的 EditMode/PlayMode 保存恢复回归；当前全量基线为 EditMode 266 / 266、PlayMode 22 / 22 通过（Unity 2022.3.62f3c1，2026-07-22）。
- [x] 完成主档损坏、备份恢复、真实进程退出、战斗中断和双分辨率人工验收；验收通过并以本次提交建立新候选（2026-07-22）。

## P0：阶段 9B 视听表现纵向切片

- [x] 完成阶段 9B 技术与制作方案、12 张样板范围和资产盘点表，见 `phase-9b-presentation-vertical-slice-technical-design-v0.1.md` 与 `phase-9b-asset-inventory-v0.1.md`。
- [x] G0：冻结工程候选 `5545a4e`、Unity 2022.3.62f3c1、内容身份 5.5.0 / 8B.1、Windows x64 目标、双分辨率和 266 / 266 EditMode、22 / 22 PlayMode 基线。
- [x] G0：冻结 9B 交付/非目标、14 张 Before 截图及 SHA-256、字体、运行时导出、分层源文件和 Git LFS 策略，见 `phase-9b-g0-baseline-v0.1.md`。
- [x] G0：建立 `phase-9b-asset-source-ledger-v0.1.md`；当前生成式插画与卡框仅限内部工程样板，生产许可未确认前不得标记 Runtime Ready 或用于最终发布。
- [ ] G1：冻结纯“旅团绘本（Wandering Storybook）”Style Tile。
  - [x] 冻结候选方向：角色、场景、材质、地图、状态和交互信息统一使用旅团绘本语言；明确排除奥术制图、云上庆典、虹彩玻璃、厚重金属框与暗黑收藏卡语言。
  - [x] 输出并选定统一视觉母版 v0.3，见 `ui-concepts/phase-9b/style-tiles/style-tile-wandering-storybook-v0.3.png`；覆盖三流派锚点、天穹契约者金色、不熄炉王转译、Full/Compact、关键词图标、卡牌状态、按钮、弹窗、地图节点、节点状态、9-slice、材质和明暗可读性。v0.2 混合方案仅保留为落选对照，不作为后续生产依据。
  - [x] 冻结关键词展示上下文：商店商品与手牌继续使用完整卡牌；正式战斗区改用“拱顶旅团肖像立牌”，常驻只显示立绘、攻击和生命，Hover 或点击锁定时再展示完整卡牌/详情；关键词通过立牌空间分区静默态与事件动画表达，永久成长不设专属图标。
  - [x] 输出战斗关键词与事件表现结构线框 v0.1，见 `ui-concepts/phase-9b/battle-keyword-validation-v0.1/`；该版只保留结构参考，不作为视觉评审依据。
  - [x] 输出旅团绘本战斗关键词高保真验证 v0.2，见 `ui-concepts/phase-9b/battle-keyword-validation-v0.2/`；在正式战斗界面和真实 160×240 卡牌上覆盖嘲讽/亡语/溅射书签、`+N` 溢出、单/双/三关键词、三关键词加护盾、四关键词溢出、护盾完整/破裂/消失及六个逐帧关键姿势。
  - [x] 完成无关键词战斗单位容器比较并选定“拱顶旅团肖像立牌”；圆形绘本徽章与不规则羊皮纸立像只保留为落选对照，见 `ui-concepts/phase-9b/battle-unit-container-options-v0.1/`。
  - [x] 冻结立牌关键词空间语言：护盾覆盖肖像表面，亡语使用拱顶上方裂蜡封，嘲讽仅强化底座挑战毯并取消侧盾肩，溅射只在攻击圆章上方显示小标记、攻击时播放独特事件动画。
  - [x] 完成立牌交互验证 v0.4，覆盖默认、Hover 完整卡牌、点击锁定详情和目标选择，见 `ui-concepts/phase-9b/arched-standee-interaction-validation-v0.4/`。
  - [x] 完成 GPT Image 立牌运行资产拆分 v0.5，输出透明立牌框、攻防圆章、亡语蜡封、溅射标记、嘲讽底座与护盾覆盖层，见 `ui-concepts/phase-9b/arched-standee-runtime-assets-v0.5/`。
  - [ ] 在 Unity 中建立 `PF_BattleStandee` Prefab：完成肖像 Mask、攻防数据绑定、四类关键词锚点、Hover 完整卡牌、点击锁定详情和目标选择接线。
  - [ ] 将 v0.5 运行资产接入 `PresentationSpriteCatalog` / Theme，校准透明边缘、Screen/Additive 混合、1×/2× 分辨率锚点和事件动画层级。
  - [ ] 增加 EditMode / PlayMode 回归并导出双分辨率截图：验证五单位并排、四关键词组合、详情框防碰撞、目标选择和战斗日志无遮挡。
  - [ ] 评审共同风格：确认画面温暖轻快但不低龄，Boss、亡语和死亡反馈仍保留威胁感。
  - [ ] 评审流派区分：在 5 秒内仅凭轮廓、主色和形状语言区分铸魂、荒灵与星契，并确认旅团中立内容不会混淆三流派。
  - [ ] 评审信息可读性：在 Full 240×360、Compact 160×240、商店、战斗和选择层验证名称、等级、攻防、关键词、进度与状态覆盖。
  - [ ] 评审金色表现：确认局部暖金墨线、金箔印章和纸面微光足够珍贵，同时保留原流派色和文本对比度。
  - [ ] 评审组件系统：确认卡框、布标、按钮、弹窗、地图节点、六枚关键词图标和五种节点状态可由共享 Sprite、9-slice 与状态参数复用，不依赖单卡分支。
  - [ ] 评审批量生产成本：记录插画、修图、导出、Unity 接入和验收的样板工时；确认来源、许可、版本与哈希可追溯。
  - [ ] G1 退出门：以上评审全部通过并记录结论后，才允许进入 G2；若任一项不通过，只返工 Style Tile，不制作剩余 9 张样板随从。
- [ ] G2：完成 ArtId → Sprite Catalog → CardView、Theme、回退/诊断资源、12 张样板随从、3 个 Token、4 张法术和 3 件遗珍图标。
- [ ] G3：完成 MainMenu、Shop、Run/Map、Battle、Choice/System Menu 换肤，接入通用 VFX、AudioMixer、3 套 BGM、P0 音效和音量设置。
- [ ] G4：完成全量自动化、正式链路双分辨率、存档恢复、跳过/2×、两机性能基线和至少 5 名外部试玩者验收。

## 暂缓

- [ ] 超出阶段 5 白名单的任意脚本化效果 DSL。
- [ ] 微信小游戏打包。
- [ ] 全量正式美术和动画（暂缓；Phase 9B 样板范围已启用）。
