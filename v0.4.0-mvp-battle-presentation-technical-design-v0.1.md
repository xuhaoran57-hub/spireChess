# v0.4.0 MVP 战斗动画与特效美术技术方案 v0.1

- 日期：2026-08-05
- 状态：方案冻结，待实现
- 视觉基线：Bright Wandering Storybook / 暗色奇幻棋盘
- 前置实现：`phase-7b-battle-ui-technical-design-v0.1.md`、
  `phase-9b-presentation-vertical-slice-technical-design-v0.1.md`

## 1. 决策与目标

暂停三职业真实单局平衡候选和 S1/S2。先把战斗表现从“能读懂结算”提升到“愿意连续
玩完一局”的 MVP 水平，再邀请人工试玩。该阶段不修改任何卡牌规则、数值、牌池、
敌人阵容、随机流或存档格式。

MVP 关闭时，未参与实现的玩家应当只看画面就能辨认：

1. 谁发起了攻击、谁受到伤害，以及伤害轻重；
2. 护盾获得、格挡和破裂；
3. 嘲讽、亡语、溅射和立即攻击的触发；
4. 法术造成的是伤害、成长、护盾还是召唤；
5. 单位死亡、Token 消散、召唤入场和最终胜负。

## 2. 当前基线与问题

现有实现已经具备稳定的播放骨架：

- `BattleSimulator.SimulatePlayback()` 输出十类结构化事件；
- `BattleScreenView` 支持 1×/2×、跳过、立牌突进、受击抖动、淡出和缩放；
- `PresentationFxPool` 提供固定容量的文字飘字；
- `PresentationAudioCatalog` 已建立通用战斗 Cue 映射。

当前不足不是规则事件缺失，而是表现全部落在“位移 + 纯色闪屏 + 文字”上：

- 没有攻击轨迹、接触点、冲击线、碎片或法术形状；
- 普通攻击、反击、溅射和效果伤害共用同一种受击反馈；
- `BattlePlaybackEvent` 没有携带 `effectId/trigger/action`，View 无法可靠区分法术与
  关键词触发；
- 护盾、亡语、溅射只有常驻徽章，没有触发瞬间；
- 没有短暂停顿、棋盘位移和分级反馈，重击与轻击体感相同；
- 现有浮字是带矩形底的文本卡片，不足以承担主要 VFX。

因此本阶段复用现有播放链，不重写战斗 UI，也不引入 Timeline、骨骼动画或完整
Shader/VFX Graph 管线。

## 3. MVP 范围

### 3.1 包含

- 攻击前摇、突进、攻击轨迹、命中爆点、受击闪白、轻重分级、短暂停顿和棋盘震动；
- 护盾生成、格挡和破裂碎片；
- 成长、伤害、护盾、召唤四类通用法术语言；
- 嘲讽、亡语、溅射、立即攻击四类战斗关键词触发；
- 普通单位死亡、Token 消散、召唤入场；
- 三流派色彩变体；
- 1×/2×、跳过、重载和连续亡语/召唤下的清理与并发上限；
- 一个固定的 `MVP VFX Showcase` 战斗预设、双分辨率截图和短视频取证。

### 3.2 不包含

- 单卡专属动画、角色骨骼、3D 摄像机、Timeline、后处理栈；
- 为 83 张卡分别制作特效；
- 正式音频母带、配音和每卡专属 Cue；
- 商店、地图和日记页面的全面动效重制；
- 平衡、内容或行为规则调整。

## 4. 美术语言

MVP 使用现有 UGUI 生成固定容量的光带、菱形爆点、短线、环形刻度和碎片。首版不依赖
新增外部位图；颜色、形状和时序稳定后，再把同一槽位替换为正式透明 Sprite 或材质。

| 语义 | 主色 | 形状 | 动态 |
| --- | --- | --- | --- |
| 物理攻击 | `#FFD15A` 金黄 + `#FFF3C0` 暖白 | 斜切光带、尖锐放射线 | 快进快出、接触点集中 |
| 伤害 | `#FF5146` 赤红 | 菱形爆点、短裂纹 | 瞬时放大后快速衰减 |
| 护盾 | `#64C7FF` 天青 | 环、六边切角、玻璃碎片 | 生成向外扩张，破裂向外飞散 |
| 成长 | `#72D879` 苔绿 + 金色 | 上升刻度、叶形短线 | 呼吸式上升，不使用受击抖动 |
| 死亡/亡语 | `#8D72B8` 暗紫 | 印章、灰烬方片、向内收束 | 先压暗，亡语亮印，再消散 |
| 铸魂 | `#E46A32` 熔橙 | 厚重环、火星短线 | 低频、少而重 |
| 荒灵 | `#69B86B` 苔绿 + `#D6D36A` 暖黄 | 弧线、叶片、孢子点 | 柔和呼吸、向上生长 |
| 星契 | `#6EC5E9` 星青 + `#A986FF` 折光紫 | 轨道环、棱形、细刻度 | 旋转、折射式错位 |

约束：任何全屏闪光峰值 Alpha 不超过 `0.22`；VFX 不遮挡攻防数字超过 `100ms`；
Compact 立牌上的常驻关键词徽章在特效结束后必须保持可读。

## 5. 动画和打击节奏

所有时间均为 1× 基线；2× 等比压缩，跳过立即归位并清空池。

### 5.1 普通攻击

1. 前摇 `60ms`：攻击者缩至 `0.96`，目标/嘲讽轮廓脉冲；
2. 突进 `90ms`：攻击者向目标移动 `54px`，产生三层斜切光带；
3. 接触 `35ms`：命中爆点、目标闪白、轻微棋盘位移；
4. 回弹 `110ms`：攻击者回位，目标完成衰减抖动；
5. 浮字 `420ms`：伤害值从接触点上移，不使用大面积矩形底。

轻击停顿 `25ms`、重击 `45ms`、致命/强反馈 `60ms`。停顿只暂停表现协程；战斗
规则已经结算，不修改 `Time.timeScale`。

### 5.2 反馈分级

| 等级 | 条件 | 棋盘震动 | 爆点数量 | 浮字 |
| --- | --- | --- | --- | --- |
| 轻 | 非致命小伤害 | `2px` | 4 | Normal |
| 重 | 伤害达到目标基础生命的一半或破盾 | `5px` | 8 | Strong |
| 关键 | 单位死亡、Boss 终结、强制关键词链 | `8px`，不超过 `120ms` | 12 | Critical |

表现层只读取事件携带的实际数值和卡面基础身材，不重新计算伤害或胜负。

## 6. 事件到特效映射

| 播放事件 | MVP 反馈 | 备注 |
| --- | --- | --- |
| `CombatStarted` | 两阵营短扫光、棋盘聚焦 | 不做长开场，`300ms` 内结束 |
| `AttackStarted` | 前摇、突进、轨迹；立即攻击使用更窄更快的金线 | 攻击者与目标必须用实例 ID 定位 |
| `DamageApplied` | 接触爆点、闪白、分级震动和伤害数值 | `wasBlocked` 不播放生命损失反馈 |
| `ShieldGained` | 天青环从立牌中心扩张、护盾层淡入 | 常驻护盾徽章仍由 Standee 负责 |
| `ShieldLost` | 环先压缩再碎裂为 6 个切片 | 格挡伤害值使用蓝色，不使用红色 |
| `StatsChanged` | `+X/+Y`、上升刻度和流派色辉光 | 负值改用红色收缩，不播放成长音形 |
| `UnitDied` | 普通单位压暗破碎；Token 更快地化为小片 | 死亡必须先于后续亡语结果可见 |
| `UnitSummoned` | 槽位环、竖向光束、`0.72→1.08→1` 入场 | 连续召唤复用池，不覆盖前一槽位 |
| `EffectTriggered` | 来源印章/符文短显，随后播放结果事件 | 本阶段新增的纯表现事件 |
| `CombatEnded` | 胜负色扫光和结果层 | 不重复提交结果 |

## 7. 法术与关键词映射

### 7.1 法术/效果动作

`BattlePlaybackEvent` 增加只读可选字段 `EffectId`、`EffectTrigger`、
`EffectAction`、`IsImmediateAttack` 和 `IsSplashDamage`。这些字段只用于表现，不进入
规则随机、存档或平衡哈希。

| Action | 表现 |
| --- | --- |
| `DealDamage` | 星青/熔橙投射线到目标，接红色爆点 |
| `ModifyStats`、`GainAttackDifference`、`GainFlourish` | 目标脚下成长环和 `+X/+Y` |
| `AddShield`、`AddKeyword:Shield` | 天青护盾环和六边切角 |
| `SummonToken` | 来源印章亮起，目标槽位开启召唤环 |
| `ImmediateAttack` | 来源金线闪烁后直接进入快速攻击 |
| 其他已有 Action | 使用流派色的通用 `EffectTriggered` 印章，不静默无反馈 |

### 7.2 关键词

| 关键词 | 常驻 | 触发瞬间 |
| --- | --- | --- |
| 嘲讽 `Taunt` | 现有底座 | 被选为目标时底座扩张一次，强调“被迫攻击” |
| 护盾 `Shield` | 现有覆盖层 | 获得时成环，格挡时亮白，失去时碎裂 |
| 亡语 `Deathrattle` | 现有紫色印章 | 死亡淡出前印章放大并亮起，结果事件随后播放 |
| 溅射 `Cleave` | 现有侧边标记 | 攻击轨迹改为宽弧，相邻目标使用较小爆点 |

## 8. 最小代码结构

不建立通用特效 DSL。只增加一个战斗专用组件和少量事件元数据：

1. `BattleImpactFxLayer`：固定池管理光带、爆点、环、碎片和浮字；最多 32 个并发条目；
2. `BattlePlaybackEvent`：增加 `EffectTriggered` 和可选表现元数据；
3. `BattleSimulator`：在效果真正执行时记录元数据，不改变执行顺序；
4. `BattleScreenView`：把事件映射到 VFX、短暂停顿和棋盘震动；
5. `BattleStandeeView`：增加闪白、关键词脉冲和强制归位接口；
6. `BattleUiPrefabBuilder`：生成并序列化 `ImpactFxLayer` 与棋盘运动根节点。

`PresentationFxPool` 暂时保留给文字反馈，待新浮字稳定后再决定是否合并；本阶段不顺手
重构商店、地图或通用 UI。

## 9. 实施批次

### FX-A：打击感基础（最高优先级）

> 2026-08-05 状态：工程实现已完成，保持“实现待复验”。已接入固定 32 节点图形池、
> 攻击前摇/拖尾、命中爆点、受击闪白、轻重 hit-stop、棋盘震动、无矩形底伤害浮字、
> 普通/Token 死亡差异和中断归位。当前机器 Unity 无有效许可，尚未完成 Prefab 重建、
> 全量 EditMode/PlayMode 与 30–60 秒 Player 对比视频，因此不得标记正式关闭。

- 普通攻击轨迹、接触爆点、闪白、轻重分级、短暂停顿和棋盘震动；
- 伤害浮字去掉大矩形底；
- 死亡/Token 消散；
- 跳过、2× 和中断归位回归。

完成后先录制 30–60 秒对比视频；若普通攻击仍无接触感，不进入下一批。

### FX-B：法术和关键词语言

- 新增 `EffectTriggered` 元数据；
- 护盾、成长、法术伤害、召唤；
- 嘲讽、亡语、溅射、立即攻击；
- 铸魂、荒灵、星契三套颜色/形状变体。

### FX-C：样板与人工试玩门

- 固定 `MVP VFX Showcase` 预设覆盖十类事件和四个关键词；
- 1920×1080、1920×1200 截图和视频；
- 1×/2×、跳过、五轮嵌套亡语/连续召唤压力复验；
- 先由项目负责人完成 1 次内部签字，再启动外部人工试玩。

## 10. 自动化与性能门禁

- 非播放 `Simulate()` 不生成表现事件，战斗最终状态和确定性保持不变；
- 新增事件元数据与实际 `EffectConfig` 一致；
- 被格挡伤害没有红色生命损失 VFX；
- 每类 MVP 事件均能解析为唯一稳定的 VFX ID；
- `SnapAndClear()` 后池、棋盘位移、立牌颜色/缩放和动画状态全部归零；
- 所有 VFX `raycastTarget=false`，不阻塞按钮和立牌交互；
- 同屏十卡、五轮嵌套亡语/连续召唤后池不扩容、无残留实例；
- 1920×1080、1920×1200 不越过战斗棋盘和日志安全区；
- 全量 EditMode/PlayMode 通过后才生成 Player 验收包。

MVP 性能预算：固定池不超过 32 个可见条目；单事件最多 12 个图形；不在播放循环中
加载资源；不修改 `Time.timeScale`；不使用每帧 LINQ 或运行时 `Instantiate/Destroy`。

## 11. 人工验收

先用 3 名内部观察者完成表现验收，不评价数值平衡：

1. 不看日志指出攻击者、目标和伤害轻重；
2. 分辨生命受损与护盾格挡；
3. 说出嘲讽、亡语和溅射何时触发；
4. 区分伤害、成长、护盾和召唤法术；
5. 1× 看完整场后愿意继续下一场，且无“等动画”的明显烦躁点。

三人中至少两人正确完成前四项，且没有共同的 P0/P1 可读性问题，才允许恢复三职业
真实单局与外部试玩。任何表现问题先修事件映射和时序，不通过增加教程文字掩盖。

## 12. 完成定义

- FX-A、FX-B、FX-C 全部关闭；
- 普通攻击、法术、四个关键词、死亡和召唤均有非文字主导的可辨识反馈；
- 双分辨率、压力场景、跳过、2×、重载和全量测试通过；
- 项目负责人签字确认 MVP 表现足以启动人工试玩；
- 平衡配置、规则和已有 S0 证据未被修改。
